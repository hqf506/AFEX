'use client'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import type { ReactNode } from 'react'
const links = [['نظرة عامة','/developer'],['المستخدمون','/developer/users'],['المنشآت','/developer/tenants'],['تذاكر العملاء','/developer/support'],['سجل العمليات','/developer/audit'],['أدوات المطور','/developer/tools']] as const
export function DeveloperShell({children}:{children:ReactNode}){const pathname=usePathname();const wide=pathname==='/developer/support'||pathname.startsWith('/developer/support/');return <div dir="rtl" className="min-h-screen overflow-x-hidden bg-[#030714] text-white"><div className={`mx-auto grid w-full gap-4 p-3 sm:p-4 lg:grid-cols-[230px_minmax(0,1fr)] lg:px-5 lg:py-5 ${wide?'max-w-[1920px] 2xl:px-6':'max-w-[1700px]'}`}><aside className="min-w-0"><div className="rounded-[24px] border border-cyan-300/15 bg-[#07111f]/90 p-3 backdrop-blur-xl lg:sticky lg:top-5"><div className="mb-3 rounded-2xl border border-emerald-300/20 bg-emerald-300/10 p-3"><p className="text-xs font-black text-emerald-200">وضع المطور</p><p className="mt-1 text-[11px] leading-5 text-slate-400">صلاحيات منصة — العمليات مسجلة</p></div><nav className="grid grid-cols-2 gap-1 sm:grid-cols-3 lg:grid-cols-1">{links.map(([label,href])=>{const active=pathname===href||(href!=='/developer'&&pathname.startsWith(`${href}/`));return <Link key={href} href={href} className={`rounded-xl border px-3 py-2.5 text-sm font-bold transition ${active?'border-cyan-300/30 bg-cyan-300/10 text-cyan-100':'border-transparent text-slate-400 hover:bg-white/[0.05] hover:text-white'}`}>{label}</Link>})}</nav><Link href="/" className="mt-3 block rounded-xl border border-white/10 px-3 py-2.5 text-center text-xs font-bold text-slate-300">العودة إلى AFEX</Link></div></aside><main className="min-w-0 overflow-x-hidden">{children}</main></div></div>}
