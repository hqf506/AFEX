# A2.4C.10 PowerShell 5.1 Deterministic Runner-Fixture JSON Compatibility Correction

Decision: `POWERSHELL_JSON_COMPATIBILITY_CORRECTED`

## Corrections

- Removed indentation, property-line, newline, and property-order dependence from retained-results validation.
- Added a Windows PowerShell 5.1-compatible structural token scan that extracts decoded top-level keys while ignoring property-like text in values and nested structures.
- Preserved fail-closed duplicate protection by rejecting every repeated top-level key before `ConvertFrom-Json` parsing.
- Added post-parse top-level object and mandatory object, array, string, and integer shape checks.
- Expanded source-contract fixtures for two-space, four-space, native PowerShell 5.1, compact, reordered, CRLF, and LF JSON; malformed JSON; missing and duplicate properties; wrong schema, run ID, authorization marker, completion marker, and final status; incorrect property type; and the existing native-exit/timeout state matrix.

## Validation boundary

Only the source-contract functions were loaded from their parsed AST and evaluated in Windows PowerShell 5.1. The operational runner path, child-process launch, harness, SQL, network, database, provider, and package paths were not executed.

Runtime evidence, provider authority, operator inputs, dependency installation, and execution authorization remain absent. Core V2 remains hard-disabled.

A24C10_900_POWERSHELL_51_JSON_COMPATIBILITY_CORRECTED
