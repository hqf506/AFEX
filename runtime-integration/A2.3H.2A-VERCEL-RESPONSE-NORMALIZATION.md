# A2.3H.2A Vercel Response Normalization

Status: PROVIDER RESPONSE ACCEPTED — RUNTIME PROOF STILL REQUIRED

| Provider statement | Classification | Interpretation | Gates |
|---|---|---|---|
| Supabase Transaction Pooler is strongly preferred over direct PostgreSQL for Vercel application traffic | RECOMMENDED | Confirms provider topology guidance, not backend pinning or role semantics | G3, G6, G8 |
| Direct PostgreSQL may exhaust database limits during serverless spikes | RECOMMENDED | Supports rejecting unbounded direct connections | G8 |
| Create one global node-postgres Pool outside the request handler | RECOMMENDED | Supports process-level reuse; does not prove safe role/session cleanup | G5, G7, G8 |
| `attachDatabasePool` is available to manage idle connections before Function suspension | CONFIRMED_SUPPORTED | Confirms Vercel lifecycle integration API availability | G5, G7, G12 |
| Use a short idle timeout, with approximately 5000 ms offered as an example | RECOMMENDED | Candidate tuning input only; not a frozen value | G5, G8 |
| Do not force maximum pool size to 1 because Functions may handle concurrent requests | RECOMMENDED | Final maximum must remain bounded and evidence-based, greater than 1 | G8 |
| Persistent prepared statements are not supported through transaction pooling | CONFIRMED_UNSUPPORTED | Future client must disable or avoid persistent prepared statements | G7, G11 |
| Temporary tables and notification listeners cannot persist across transaction-pooled queries | CONFIRMED_UNSUPPORTED | These session features are forbidden in the future adapter | G5, G7 |
| Example uses `pg`, `Pool`, `attachDatabasePool`, a database URL variable, checkout, and release | DOCUMENTATION_ONLY | Demonstrates recommended integration shape; it is not AFEX implementation approval | G5, G7, G8, G12 |
| Outbound PostgreSQL use from Vercel Node Functions is contemplated and supported by the supplied guidance | CONFIRMED_SUPPORTED | Advances TCP feasibility but does not prove IP family or TLS behavior | G6, G7 |
| Transaction backend pinning | REQUIRES_RUNTIME_TEST | Not answered; Supabase confirmation and isolated proof remain mandatory | G3 |
| `SET ROLE` and `SET LOCAL ROLE` | OUT_OF_SCOPE | Vercel response does not establish database/pooler role semantics | G4 |
| Commit/rollback/cancel/disconnect role sanitation | OUT_OF_SCOPE | Pool lifecycle guidance does not prove PostgreSQL role-state cleanup | G5, G10 |
| Transaction advisory locks | OUT_OF_SCOPE | No evidence supplied | G11 |
| Exact pool size, maximum Function concurrency, region, memory | AMBIGUOUS | Guidance is qualitative; project-specific values remain missing | G6, G8, G12 |

## Evidence boundary

The response is provider-reported evidence. It does not prove Supavisor behavior, backend identity, role reset, cancellation safety, or AFEX Runtime correctness. No credential value, connection string, project secret, or business data is retained in this normalized record.
