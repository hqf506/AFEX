# A2.4B.4 Independent SQL, Harness, and Execution-Safety Review

Status: CORRECTION_REQUIRED

## Executive verdict

The 13-artifact package is coherent as an early design but is not safe to freeze or execute. The harness can report semantic failures as passes, multiple activation-blocking tests are absent, the plain-Node launch cannot resolve the `server-only` import in the current dependency state, and SQL/runner attestations do not yet implement the promised fail-closed privilege and execution controls. No execution or package installation is authorized.

## Preconditions and coverage

Branch, HEAD, origin, ahead/behind, visible 13-file manifest, zero staged files, absence of active Git operations, and `git diff --check` all matched the required baseline. Every line of the four SQL files, PowerShell runner, TypeScript harness, and seven documents was reviewed against A2.1–A2.4B.2 and P2D.19/P2D.20 boundaries.

## Component decisions

| Component | Decision |
|---|---|
| Test setup SQL | CORRECTION_REQUIRED |
| Read-only attestation SQL | INCOMPLETE |
| Cleanup/revocation SQL | CORRECTION_REQUIRED |
| Future Production design SQL | SAFE AS NON-EXECUTABLE DESIGN ONLY |
| PowerShell runner | CORRECTION_REQUIRED |
| TypeScript harness | CORRECTION_REQUIRED |
| Operator runbook | CORRECTION_REQUIRED |

## Findings matrix

| ID | Severity | Exact path and line | Issue | Failure or exploit scenario | Violated contract | Required correction | Execution impact | Commit impact |
|---|---|---|---|---|---|---|---|---|
| F01 | BLOCKER | `database-reconciliation/core-v2/P2D/A2.4B.3-ISOLATED-DIRECT-EXECUTE-HARNESS.ts:66-73,87-134` | `fixedCase` marks every non-throwing operation passed; denial booleans, PID equality, timeout result, identities, and probe fields are never asserted. | Allowed table/function access, changed backend PID, or a statement that did not time out is recorded with `passed=true`. | Fail-closed pass/fail contract. | Add exact typed assertions per case and fail on every unexpected value; distinguish expected SQLSTATE from arbitrary errors. | Blocks all harness execution. | Harness, docs, manifest hashes. |
| F02 | BLOCKER | `database-reconciliation/core-v2/P2D/A2.4B.3-ISOLATED-DIRECT-EXECUTE-HARNESS.ts:103-134`; `runtime-integration/A2.4B.3-HARNESS-DESIGN.md:20-25` | Mandatory two-client lock contention, explicit cancellation controller, abrupt disconnect, unknown commit outcome, pool exhaustion, LOGIN disablement, and EXECUTE revocation are not implemented. | Activation gates could be declared tested without exercising their failure modes. | A2.4B.2 mandatory runtime-test inventory. | Implement fixed, bounded cases and evidence assertions, or explicitly remove them from the executable package and retain the package as incomplete design. | Blocks G5, G8–G12 closure. | Harness, runner, runbook, evidence contract, gate map, manifest. |
| F03 | BLOCKER | `database-reconciliation/core-v2/P2D/A2.4B.3-ISOLATED-DIRECT-EXECUTE-HARNESS.ts:1`; `database-reconciliation/core-v2/P2D/A2.4B.3-RUN-ISOLATED-DIRECT-EXECUTE-TESTS.ps1:32-35` | Runner launches the file in plain Node, but `server-only` is not installed/resolvable in the current repository runtime. | The process fails before any case; TypeScript type-check did not prove runtime module resolution. | Runnable isolated harness and dependency-boundary accuracy. | Remove the Next-only poison import from the standalone harness and enforce server-only isolation structurally, or pin/install a reviewed compatible dependency in a later authorized phase. | Harness cannot currently launch. | Harness, runner assumptions, dependency docs, manifest. |
| F04 | HIGH | `database-reconciliation/core-v2/P2D/A2.4B.3-DIRECT-EXECUTE-TEST-SETUP.sql:3-17`; cleanup SQL `:3-7` | psql guards prove variable existence, not an exact authorization value; validity is not parsed, bounded, or proven future/short. | An empty/arbitrary defined authorization value permits mutation; `VALID UNTIL` may be indefinite, stale, or excessively long. | Explicit authorization and short-lived credential contract. | Compare exact authorization tokens and validate expiry format, future bound, and maximum lifetime before mutation. | Blocks setup/cleanup execution. | Setup, cleanup, runbook, manifest. |
| F05 | HIGH | Setup SQL `:21-43` | Installer preflight checks only UTF8/version/conflicts and SET capability for one owner. It does not prove CREATEROLE, schema CREATE, role administration, object creation, ownership-transfer, grant/revoke, or managed-role availability. | Setup can fail after entering the transaction or behave differently under Supabase-managed restrictions. | Fail-closed installer-authority contract. | Add exact authority and managed-role preflight before mutation, including expected current/session identity. | Blocks setup execution. | Setup, attestation, runbook, manifest. |
| F06 | HIGH | Attestation SQL `:42-78` | ACL verification is not exact: no default ACL causality, column ACL inventory, unrelated-function effective inventory, overload inventory, direct unexpected grantee inventory, or effective SELECT check across relations. | LOGIN may retain PUBLIC/default EXECUTE or SELECT access and still pass. | Direct-EXECUTE exact-only and zero-object-access contract. | Build exact direct/effective function, schema, table, column, sequence, ownership, membership, and default-ACL inventories with fail-closed expected sets. | Blocks G1 privilege attestation. | Attestation, setup verification, manifest. |
| F07 | HIGH | Attestation SQL `:71-75` | The broad relation scan omits SELECT for non-sequences and mixes table privilege checks with sequence rows. | Read access can escape detection; catalog behavior may error or misclassify sequences. | Table/sequence denial and PostgreSQL 17.6 compatibility. | Split relation and sequence scans by relkind and verify every applicable privilege separately. | Blocks attestation execution. | Attestation and manifest. |
| F08 | HIGH | Cleanup SQL `:9-38` | Cleanup requires complete setup, does not handle partial installation, performs no session discovery/termination, and disables LOGIN inside the same transaction as drops. | A failed drop rolls back `NOLOGIN PASSWORD NULL`, leaving the credential active; partial setup cannot be safely reconciled. | Cleanup after every failure and credential invalidation contract. | Design staged fail-closed shutdown: externally revoke credential/disable LOGIN, terminate/quarantine sessions, classify exact partial states, then transactionally remove only expected objects and attest absence. | Blocks cleanup execution. | Cleanup, runbook, manifest. |
| F09 | HIGH | Runner `:17-27,32-54` | No repository-root proof, approved evidence-root containment, Git-ignore proof, pinned hash comparison, timeout, completion-marker validation, or safe summary on launch failure. | Arbitrary paths can receive evidence; a hung child waits forever; changed harness or incomplete run may be accepted. | Runner integrity, evidence retention, timeout, and fail-closed requirements. | Add normalized containment/root checks, pinned manifest validation, bounded process timeout/termination, marker/result validation, and finally-safe summary creation. | Blocks runner use. | Runner, runbook, manifest. |
| F10 | HIGH | Harness `:40-45,83` | Host check accepts any hostname containing `pooler`; `sslmode=require` is treated as TLS proof without explicit certificate/hostname validation or reviewed endpoint allowlist. | A misdirected endpoint or non-verifying TLS configuration can pass configuration validation. | Transaction-pooler-only and verified TLS contract. | Validate an exact approved masked topology/host pattern and construct reviewed TLS options with certificate and hostname verification. | Blocks G6 closure. | Harness, config design, manifest. |
| F11 | HIGH | Harness `:87-97` | The value labeled `after` is captured before COMMIT; no post-commit/reacquisition identity observation exists. | Transaction-end identity/cleanup is never proven. | Identity-before/during/after and reuse-cleanup evidence. | Capture post-commit on a valid client and after a new checkout; assert expected session/effective identities. | Blocks G3/G5 evidence. | Harness and evidence schema. |
| F12 | HIGH | Harness `:76-78,98-102` | Any error proves denial; SQLSTATE and object-specific permission failure are not checked. | Missing object, syntax error, wrong schema, or broken connection is misreported as successful privilege denial. | Exact denial-test contract. | Require the expected insufficient-privilege SQLSTATE and separately prove target existence/ownership via attestation. | Blocks privilege-denial tests. | Harness and evidence contract. |
| F13 | HIGH | Harness `:113-125` | Timeout case always returns normally and is therefore passed even when `timedOut=false`; it is a statement timeout, not a cancellation controller. | Cancellation behavior remains entirely untested while evidence says quarantine succeeded. | Cancellation/timeout and semantic assertion contracts. | Assert timeout SQLSTATE, record rollback state, verify destroyed connection, and implement a separately controlled cancellation case. | Blocks G5/G10. | Harness, docs, manifest. |
| F14 | MEDIUM | Setup SQL `:59-85` | Test objects use the Production function owner, increasing its owned-object/dependency surface for a disposable test. | Cleanup failure leaves test objects under a privileged Production owner. | Minimal blast radius and disposable isolation. | Justify and attest this owner choice or introduce an independently reviewed disposable owner with no Production reachability. | Requires architecture review before setup. | SQL/design docs. |
| F15 | MEDIUM | Runner `:20`; harness `:84,138-142` | Runner directory ID and JSON run ID are different and no binding is asserted. | Evidence from one process can be mis-associated with another directory/summary. | Unique, deterministic evidence identity. | Pass one run ID into the harness and assert it in filenames, summary, JSON, and hashes. | Weakens evidence provenance. | Runner, harness, manifest. |
| F16 | MEDIUM | Harness `:138-143`; runner `:42-48` | Evidence omits dependency/npm versions, stdout/stderr hashes, process exit code in JSON, denial/contamination matrices, lock contention, quarantine details, cleanup, absence proof, and reviewer verdict. | Evidence cannot satisfy the normative contract or independently close gates. | A2.4B.3 evidence contract. | Emit the complete schema or narrow the documented evidence claim until implemented. | Blocks independent test verdict. | Harness, runner, docs, manifest. |
| F17 | MEDIUM | Attestation SQL `:25-30` | Ownership scan covers database/schema/relation/function only, not all ownable object classes or SETtable ownership paths. | Unexpected ownership can remain outside the checked catalogs. | Zero ownership and transitive authority contract. | Inventory every relevant ownable catalog class and effective owner/SET-role path for PostgreSQL 17.6. | Blocks complete identity attestation. | Attestation and manifest. |
| F18 | LOW | Hash manifest `:5,9-20` | The 12 listed artifact hashes/counts match, but the manifest necessarily omits its own row and has no detached signed/pinned digest artifact. | Operator can use the externally reported manifest hash, but the package itself is not self-pinning. | Hash-manifest integrity. | Pin the manifest hash in the later reviewed runner/runbook or a detached immutable review record. | Does not alone block correction review. | Runner/runbook or external review record. |

## Two-client and controller status

| Required capability | Status |
|---|---|
| Two-client advisory-lock contention | DESIGN_ONLY |
| Separate cancellation controller | MISSING |
| Abrupt disconnect | DESIGN_ONLY |
| Unknown commit outcome | DESIGN_ONLY |
| Pool exhaustion | DESIGN_ONLY |
| LOGIN disablement | DESIGN_ONLY |
| EXECUTE revocation | DESIGN_ONLY |
| Single-client advisory lock acquisition | PARTIALLY_IMPLEMENTED |
| Statement timeout with destruction | PARTIALLY_IMPLEMENTED |
| Sequential reuse observations | PARTIALLY_IMPLEMENTED |
| Concurrent identity observations | PARTIALLY_IMPLEMENTED |

## Production-design SQL review

The Production design file is safely stopped by unconditional psql quit before its comment-only design body, contains no secret, preserves exact-signature direct EXECUTE, calls for browser/PUBLIC/service-role review, retains `afex_core_runtime` disposition review, rollback/revocation, attestation, and no activation. It is approved only as non-executable design text, never as Production SQL.

## Operator runbook review

The sequence correctly orders authorization, hashes, setup, attestation, harness, cleanup, absence proof, and independent review, and it prohibits execution before approval. It is incomplete for partial setup, pre-harness dependency pinning, exact commands/markers, safe retry prohibition, and guaranteed cleanup when setup or the runner fails before its normal evidence path. Correct it together with SQL and runner behavior.

## Provider and authority assumptions

| Assumption | Classification |
|---|---|
| Current installer can CREATE ROLE and administer the disposable LOGIN | BLOCKING / REQUIRES_PROVIDER_CONFIRMATION |
| Installer can create in `public` and transfer ownership to `afex_function_owner` | BLOCKING / REQUIRES_PROVIDER_CONFIRMATION |
| `afex_function_owner` is available and appropriate for disposable ownership | BLOCKING / REQUIRES_OPERATOR_INPUT |
| `anon`, `authenticated`, and `service_role` exist with expected managed semantics | REQUIRES_PROVIDER_CONFIRMATION |
| SQL execution channel supports required psql variables/metacommands | BLOCKING / REQUIRES_OPERATOR_INPUT |
| Transaction-pooler endpoint, credential format, and port are exact | REQUIRES_PROVIDER_CONFIRMATION |
| Verified TLS mode/certificate/hostname behavior | BLOCKING / REQUIRES_PROVIDER_CONFIRMATION |
| IPv4/IPv6 and execution location (local versus Vercel) | REQUIRES_OPERATOR_INPUT |
| Finite LOGIN/pool limits and timeout hierarchy | BLOCKING / REQUIRES_OPERATOR_INPUT |
| Credential delivery, rotation, revocation, and evidence retention | BLOCKING / REQUIRES_OPERATOR_INPUT |
| Supavisor pinning, advisory locks, cancellation, reuse, and cleanup | REQUIRES_RUNTIME_TEST |

## Security review

No credential literal, complete connection URL, JWT, private key, business identifier/data, CASCADE, unsafe dynamic SQL, caller-controlled SQL/role/schema/function target, P2D.20 call, package installation, application activation, or Git mutation was found. Direct-EXECUTE architecture and service-role rejection remain documented. The material risks are incomplete effective-privilege proof, permissive execution guards, unverified TLS/topology, semantic false passes, missing destructive controllers, and incomplete cleanup.

## Independent static validation

- PowerShell parser: 0 errors.
- Node TypeScript syntax check: PASS.
- `npx tsc --noEmit`: PASS without installing packages.
- SQL mutation classification: setup and cleanup mutate; attestation is read-only and ends in rollback; Production design quits before comment-only content.
- Setup transaction markers: one BEGIN, one COMMIT, no rollback path outside transaction exception rollback.
- Attestation: one read-only BEGIN, one marker, one ROLLBACK.
- Cleanup: one BEGIN, one COMMIT, one marker.
- Credential/full-URL scan: no literal finding.
- UTF-8 without BOM, LF, trailing-whitespace scan: PASS for all 13 artifacts.
- Hash manifest: all 12 listed rows match actual line counts, bytes, and SHA-256; manifest SHA-256 is `a0d4dd6a755f3fdca68d55f8a73a70b7cad65e2ab480ef9c5691910d1fe6ad64`.
- `git diff --check`: PASS.
- Staged files: 0.
- Runner and harness were not executed; SQL was not executed; no database/provider was contacted.

## Readiness decision

`CORRECTION_REQUIRED`

Any BLOCKER or HIGH prevents correction freeze and every form of execution approval. The next phase should be `A2.4B.5 — Fail-Closed SQL, Runner, Harness, and Evidence Correction`, limited to the exact findings above, followed by another independent read-only review.
