# A2.4C.7 Final Independent Result-Integrity Approval Rereview

Decision: `NARROW_SOURCE_CORRECTIONS_REQUIRED`

## Executive verdict

The A2.4C.6 runner control-flow correction and unified evidence projection are source-correct. Retained evidence is now validated before native-exit classification, and all final result-derived matrices are built from the same bounded projected result set. Approval is nevertheless withheld because the required source-only regression matrix was not implemented, the negative redaction fixture covers only `token` and `queryText`, and the fixed evidence-schema and harness-tool versions do not pass through the required version validator. These are narrow corrections; no architectural redesign is required.

## Repository preconditions

- Branch `master`; HEAD and `origin/master` are `4fd239784ea6d38e1744b2b62d664aa33b02496c`.
- Ahead/behind `0/0`; staged files `0`; no merge, rebase, cherry-pick, or revert is active.
- Visible paths are confined to the expected A2.4B.3 through A2.4C.6 package; no unrelated path was found.
- `git diff --check` passes.

## Review coverage

The runner, harness, evidence contract, hash manifest, blocker decision, A2.4C.3 through A2.4C.6 records, and traceability index were read completely and cross-checked against the exact three HIGH and two MEDIUM A2.4C.5 findings.

## Result-integrity findings

Decision: `RESULT_INTEGRITY_APPROVED`.

Runner lines 193-227 require both evidence files, validate digest syntax, independently recompute SHA-256, compare the digest, require each mandatory top-level field exactly once in the canonical emitted layout, parse JSON, require all mandatory properties, and bind schema version, run ID, authorization marker, completion marker, and final status. Lines 229-234 only then classify timeout, native exit, stderr, stdout marker, and PASS status. Exit zero with FAIL/INCOMPLETE fails; nonzero with valid FAIL/INCOMPLETE is integrity-validated and then fails; invalid or missing evidence fails before classification; timeout with safely retained evidence is validated and then fails. Uncertain termination fails before unsafe evidence use. No reviewed branch can produce a false PASS.

## Unified redaction findings

Decision: `UNIFIED_REDACTION_APPROVED` for production evidence behavior.

Harness lines 130-157 define the operation-specific bounded result projection. Lines 159-198 create one authoritative final evidence object, derive every identity, contamination, isolation, lock, timeout, cancellation, disconnect, semantic-assertion, and quarantine view from `safeResults`, and expose only fixed topology, runtime, hash, process, status, and review fields. The only JSON serializations are the projected negative fixture at line 209 and the authoritative evidence object at line 462. No `CaseResult`, `Error`, `process.env`, connection configuration, query text, SQL parameter, stack, or arbitrary recursive object is directly serialized. Fixed hashes and versions are validated upstream, and unapproved observation fields are omitted.

## Version-evidence findings

Decision: `VERSION_EVIDENCE_CORRECTION_REQUIRED`.

Lines 107-114 correctly enforce presence rules, a 64-character maximum, trim equality, CR/LF rejection, an approved version grammar, lowercase normalization, and `NOT_INSTALLED` for optional packages. Node, npm, `pg`, optional `@types/pg`, and optional `@vercel/functions` use this validator at lines 255-259. The evidence-schema and harness-tool values at lines 260-261 are safe fixed literals, but they bypass the validator despite the explicit requirement that schema/tool versions also be validated before serialization. This is not an immediate secret-leak path, but it is a source-contract gap.

## Negative redaction fixture findings

The fixture at lines 208-210 contains only `token` and `queryText`. Its value is a fixed synthetic marker and cannot expose a real secret. The authoritative projection does omit arbitrary unknown fields, but the required negative cases for `password`, `connectionString`, `rawHost`, `stack`, and `arbitraryPayload` are not present and therefore are not proven by the fixture. Runner scenario fixtures for nonzero valid INCOMPLETE evidence, digest mismatch, malformed JSON, and wrong run ID are also absent; the current code and evidence-contract prose implement or describe these outcomes but do not provide the required source-only regression fixture coverage.

## Traceability findings

Decision: `TRACEABILITY_APPROVED`.

The traceability index explicitly states that A2.4C.2 was a prior returned report and was not persisted. It does not recreate or infer its contents. It truthfully maps the attempted correction and independently persisted findings through A2.4C.3, A2.4C.4, A2.4C.5, and A2.4C.6.

## Status consistency

A2.4C.3 now identifies itself as a historical attempted correction and records that A2.4C.4/A2.4C.5 found three unresolved HIGH defects. A2.4C.6 is identified as the corrective phase, while independent review, runtime evidence, provider authority, operator inputs, dependency installation, execution authorization, and Core V2 activation remain absent. The package remains hard-disabled.

## Manifest findings

- Rows: `12`.
- Relative-path, line-count, byte-count, and SHA-256 mismatches: `0`.
- Missing or stale execution-package rows: `0`.
- Purpose, execution status, and review status are synchronized with the current package state.
- Self-hash treatment is explicit and non-recursive.
- Independently recomputed manifest SHA-256: `1a54b723621a646a17380618321b2aa94df54fe9ecd1f13b854f07f78a438a0e`.

## Regression review

No regression was found in the read-only attestation, bounded and checked process-tree termination, digest/JSON contract enforcement, fixed assertion identifiers, denial expected/actual SQLSTATE evidence, mandatory final-status binding, timeout hierarchy, partial-setup STOP controls, hard-disabled authorization gates, or environment restoration. The attestation retains one read-only transaction, one ROLLBACK, zero COMMIT, and zero executable mutation statements. No SQL, package, runner, or harness execution occurred.

## Static validation

- Windows PowerShell 5.1 parser errors: `0`.
- PowerShell 7: not available locally; no result claimed.
- Node TypeScript syntax validation: PASS.
- `npx tsc --noEmit`: PASS.
- Result-control-flow and authoritative-projection scans: PASS.
- Direct serialization bypass scan: PASS.
- Version scan: one narrow schema/tool validation gap recorded below.
- Negative fixture scan: incomplete coverage recorded below.
- Credential/full-URL scan: no real credential or complete connection URL found.
- UTF-8 without BOM, LF endings, and trailing-whitespace scan: PASS.
- Manifest rows/mismatches: `12/0`.
- `git diff --check`: PASS; staged files `0`.

## Findings matrix

| Severity | Exact path and line | Issue | Failure scenario | Violated contract | Required correction | Execution impact | Commit impact |
|---|---|---|---|---|---|---|---|
| MEDIUM | `database-reconciliation/core-v2/P2D/A2.4B.3-ISOLATED-DIRECT-EXECUTE-HARNESS.ts:200-211` | Required negative-redaction fixture inventory is incomplete. | A later projection change could expose `password`, `connectionString`, `rawHost`, `stack`, or `arbitraryPayload` without the mandated fixture detecting that named regression. | A2.4C.6 source-only negative coverage for every prohibited field class. | Add fixed synthetic fields for all required names and assert that neither their keys nor marker values occur after authoritative projection. | No current evidence leak was found; approval lacks the required regression proof. | Harness, manifest, correction/status record. |
| MEDIUM | `database-reconciliation/core-v2/P2D/A2.4B.3-RUN-ISOLATED-DIRECT-EXECUTE-TESTS.ps1:193-234` | Required source-only runner outcome fixtures are absent. | Future control-flow edits could move exit classification ahead of digest/JSON checks without a fixture covering nonzero valid evidence, digest mismatch, malformed JSON, or wrong run ID. | A2.4C.6 regression coverage requirements. | Extract or add a non-executing contract-validation fixture covering the mandated runner state matrix without launching the harness. | Current flow is correct, but regression approval is incomplete. | Runner/fixture source, manifest, evidence contract. |
| LOW | `database-reconciliation/core-v2/P2D/A2.4B.3-ISOLATED-DIRECT-EXECUTE-HARNESS.ts:260-261` | Fixed schema/tool versions bypass `normalizeVersionEvidence`. | A future edit could introduce a malformed fixed version without the common validator rejecting it. | Every serialized version, including schema/tool, must pass strict validation. | Pass both fixed literals through `normalizeVersionEvidence(..., true)` or a shared equivalent. | No present runtime exposure because both literals are safe constants. | Harness and manifest. |
| INFORMATIONAL | `runtime-integration/A2.4C-TRACEABILITY-INDEX.md:3-12` | A2.4C.2 remains intentionally absent. | Repository-local history cannot reproduce the original conversation report. | Truthful provenance rather than fabricated history. | None; the absence and authoritative persisted mappings are documented correctly. | No source or runtime impact. | No correction required. |

## Readiness decision

`NARROW_SOURCE_CORRECTIONS_REQUIRED`

No BLOCKER or HIGH finding remains, but the explicit regression-proof and common version-validation requirements are incomplete. This review grants no SQL execution, dependency installation, database/provider contact, Production testing, Git write, or Core V2 activation authority.

## Boundaries and next phase

No existing file was modified. No SQL, runner, or harness was executed; no package was installed; no database/provider was contacted; no credential was added; no staging, commit, or push occurred. External provider authority, operator values, dependency approval, disposable runtime evidence, cleanup/absence proof, and execution authorization remain outstanding external gates rather than source defects.

Exact next phase: `A2.4C.8 — Narrow Regression-Fixture and Schema/Tool Version-Validation Correction`, followed by one final independent source rereview.

A24C7_900_FINAL_INDEPENDENT_RESULT_INTEGRITY_APPROVAL_REREVIEW_COMPLETE
