# A2.4A.10 Expression, Spread, Class Alias, and Override Correction

Status: IMPLEMENTED — READY FOR FINAL INDEPENDENT REREVIEW

## Resolution summary

All A2.4A.9 findings are corrected within the cases reviewed in that phase. Provenance path resolution now unwraps parenthesized, assertion, non-null, satisfies, and partially emitted expressions; selects the effective sequence operand; resolves compile-time boolean conditionals; conservatively merges unresolved conditional and nullish paths; and retains final exposure coordinates. A2.4A.11 subsequently identified additional tuple-rest and alternative-spread cases outside A2.4A.10's fixture set; A2.4A.12 adds their explicit coverage.

Array/readonly-tuple registration carries callable/value provenance through the direct, nested, returned, exported, destructured, and statically sized spread cases tested here. Tuple-rest suffix containers and conditional/nullish alternative layouts are covered by the A2.4A.12 correction.

Named and anonymous variable-bound class expressions use the same static/instance/getter/field/inheritance analysis as class declarations for the direct aliases tested here. Class factories, constructor-return aliases, and namespace-bound class-expression identities require and receive separate A2.4A.12 handling.

Inheritance treats instance and static prefixes as mutually exclusive and resolves declared overrides before inheriting base members in the same context. A2.4A.12 further separates the declared-name sets so a static declaration cannot suppress instance inheritance, and an instance declaration cannot suppress static inheritance.

## Fixtures and diagnostics

New isolated fixtures cover parenthesized indexed values/calls, sequence/conditional/nullish selection, direct/nested/destructured/callable spreads, named/anonymous/static class expressions, multi-hop and namespace/CommonJS aliases, barrels, inherited unsafe members, unsafe overrides, and nine corresponding safe controls.

Diagnostics continue to use `adapter_generic_query_surface` and retain adapter root, original member, expression/container/class declaration, alias/spread/inheritance provenance, final exposure coordinates, and deterministic ordering.

## Frozen scope

No adapter, application, route, package, environment, SQL, migration, production configuration, P2D.20 mapping, acquire-only transport, raw result, authority/idempotency/replay/error/outbox/lifecycle contract, factory, fake allowlist, fallback, dual-write, or activation behavior changed.

## Validation contract

- Core V2 contracts: 295 tests.
- Scanner fixtures: 158.
- Stable rules: 19.
- Isolated coverage: 19.
- Uncovered rules: zero.
- TypeScript: PASS.
- ESLint: PASS with zero errors and six unrelated pre-existing warnings.
- Live boundary scan: PASS.
- Production build: PASS, 114 pages.
- `git diff --check`: PASS.
- Strict trailing-whitespace scan: PASS.
- Application/route adapter imports, adapter environment reads, executable P2D.20 calls, Pool/Client construction, package changes, SQL changes, feature activation, and legacy-path changes: zero.

## Remaining limitations

Arbitrary runtime-generated expressions, runtime-selected constructors/inheritance, dynamic prototype mutation, monkey-patching, evaluated/generated code, symbol-only members, nonliteral runtime imports, and information-erasing casts remain outside absolute static proof and prohibited by review policy.

A24A10_900_EXPRESSION_SPREAD_CLASS_ALIAS_AND_OVERRIDE_CORRECTED
