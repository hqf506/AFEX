# A2.4B.1 Gate Reassessment

Status: ISOLATED TEST DESIGN MAY PROCEED; RUNTIME ACTIVATION BLOCKED

| Gate | Reassessed state | Basis | Closure requirement |
|---|---|---|---|
| G1 Dedicated LOGIN | REQUIRES_RUNTIME_TEST | The disposable design defines a dedicated LOGIN, but none is created or attested | Reviewed setup, exact attributes, isolation, short-lived credential, and cleanup proof |
| G2 SET-only membership | REQUIRES_RUNTIME_TEST | Exact membership is designed; hosted creation and resulting catalog state are unproven here | Attest `SET=true`, `INHERIT=false`, `ADMIN=false`, no other membership, and later revocation |
| G3 Transaction pinning | REQUIRES_RUNTIME_TEST | Backend lending for a transaction is documented but exact PID stability is not proven | B01 passes for commit, rollback, and concurrent cases |
| G4 Scoped role activation | REQUIRES_RUNTIME_TEST | Session `SET ROLE` is rejected; `SET LOCAL ROLE` is undocumented | B02 plus all identity/reset observations pass |
| G5 Cleanup/reuse safety | REQUIRES_RUNTIME_TEST | Limited reset and possible session-state persistence prevent assumption | B03–B09 and B12 pass with zero contamination |
| G6 TLS/network | PARTIALLY_PROVEN | Vercel database connectivity and transaction-pooler direction are supported | Exact endpoint class, verified TLS mode, certificate/hostname handling, IP/region path, and safe metadata evidence |
| G7 Vercel Node compatibility | PARTIALLY_PROVEN | `pg`, global Pool, and lifecycle integration are recommended; no package or harness exists | Separately authorize dependencies, validate Node version, prohibit preparation, and pass lifecycle tests |
| G8 Capacity | PARTIALLY_PROVEN | Bounded global pooling is recommended; project budget is not frozen | Reviewed pool maximum, checkout timeout, concurrency bound, database budget, and load evidence |
| G9 Secret lifecycle | PARTIALLY_PROVEN | Short-lived disposable credentials are required but not provisioned | Approved delivery, scope, masking, rotation, revocation, evidence retention, and emergency response |
| G10 Cancellation/unknown outcomes | REQUIRES_RUNTIME_TEST | Provider guidance does not prove cleanup or outcome classification | B04–B07 and B12 pass; uncertainty always quarantines |
| G11 Advisory locks | REQUIRES_RUNTIME_TEST | Transaction lock behavior through Supavisor is not guaranteed | B10 passes for contention, commit, rollback, timeout, and disconnect; prepared statements remain prohibited |
| G12 Observability/baseline | PARTIALLY_PROVEN | Evidence fields and retention boundaries are designed only | Harness produces complete masked evidence, hashes, exit codes, cleanup attestation, and reviewer decision |

## Gate decision

- Fully closed gates: none.
- Testable now by design after separate setup/harness authorization: G1–G5, G10, G11.
- Still dependent on operational evidence and approvals: G6–G9, G12.
- Rejected mechanism: session-scoped `SET ROLE` and persistent prepared statements.

No gate authorizes package installation, SQL execution, database contact, Production credentials, application wiring, or Core V2 activation.
