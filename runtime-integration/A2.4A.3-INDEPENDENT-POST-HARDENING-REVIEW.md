# A2.4A.3 Independent Post-Hardening Review

## Executive verdict

`CORRECTION_REQUIRED`

A2.4A.2 fixed the originally demonstrated package, environment, P2D.20, role-target, fake-reachability, and fake-mutation examples. The implementation remains hard-disabled and the live repository is clean. A fresh adversarial review nevertheless found a HIGH generic-surface alias bypass explicitly covered by the rereview requirements, plus incomplete diagnostic/fixture assertions. Commit is not approved.

## Repository preconditions

- Branch: `master`.
- HEAD and `origin/master`: `e6260732cbdd5a3478fcb5f1130518c58ad7225d`.
- Staged paths: zero.
- Merge, rebase, cherry-pick, and revert state: absent.
- Every visible path belongs to A2.4A, A2.4A.1, A2.4A.2, or this review.
- Application, package, lockfile, environment, route, migration, and SQL changes: zero.
- Initial `git diff --check`: pass.

## Review coverage

Every current created or modified source and documentation file was read completely. The authorization, idempotency, replay, error, outbox, A1 freeze, A2.1 architecture, A2.2 client, A2.3 lifecycle, and A2.3H gate contracts were reread. Independent virtual repositories were used to exercise the scanner; no repository source was changed by those probes.

## Transitive package findings

The stable `adapter_forbidden_package_import` rule independently caught adapter chains reaching `pg`, `pg/*`, `postgres`, `postgres/*`, and `@vercel/functions/*` through relative paths, `@/` aliases, exports, CommonJS, literal dynamic/static-template imports, and cycles. Safe wrappers and inert strings/comments passed. Exit status was nonzero and chains were deterministic.

Residual issue: the formatted finding is anchored at the adapter root import and its description names the forbidden source file but not the forbidden source line/column (`scripts/check-core-v2-boundaries.mjs:548-554`). This misses the required exact source-location diagnostic.

## Environment-wrapper findings

PASS for enumerated forms. Dot/bracket `process.env`, destructuring, aliases, `import.meta.env`, wrappers, literal dynamic imports, CommonJS, barrels, and cycles were detected. Diagnostics include root, chain, source, and source location. Harmless strings passed. Runtime-generated/global monkey-patched environment objects remain outside static proof.

## Generic-query findings

Direct, quoted, computed literal, static-template, function-valued named properties, object type members, intersections, merged interfaces, and inherited declarations were detected for the forbidden vocabulary. Safe `acquire`, `queryLabel`, `transactionState`, documentation, and unrelated application interfaces remain allowed.

HIGH bypass: `const run = client.query; export { run }` returns zero findings. `genericSurfaceNodes` only evaluates declaration/member names (`scripts/check-core-v2-boundaries.mjs:276-310`) and does not propagate a forbidden property-access capability into an alias. Constant-derived computed names, mapped types, and equivalent object spreads also evade it even where their string key is statically resolvable. This contradicts the explicit “aliases of generic surfaces” requirement and the hardening report's claim.

## P2D.20 findings

PASS for direct, imported/destructured/property/computed/static-template aliases, literal RPC calls, aliased RPC methods, local wrappers, barrel/CommonJS/literal-dynamic wrappers, and cycles. Frozen function-name constants not invoked remain allowed. Nonliteral runtime-computed calls remain an accepted static limitation.

## Role-target findings

PASS for the seven frozen names, case/underscore/hyphen normalization, quoted/computed literal members, nesting, destructuring, parameters, wrappers, and inherited declarations. Lifecycle/internal constant strings remain allowed. Constant-derived computed and mapped-type keys share the residual static-expression limitation noted above.

## Fake allowlist findings

PASS for current enumerated import forms. Only the two exact approved scripts are skipped as roots. Production/root/intermediate barrels, arbitrary server helpers, API routes, Server Actions, non-test scripts, CommonJS, literal dynamic imports, aliases, and cycles fail with importer chains. Runtime-generated imports remain an accepted documented limitation.

## Fake immutability findings

PASS. Constructor shape/disposition validation snapshots primitive, date, byte, array, and plain-object values without caller references. Caller mutation of the original row, nested response, fingerprint bytes, or failure Error does not alter retained state. Each acquisition materializes a fresh result; mutation of it cannot affect retained results or the frozen disposition snapshot. The fake returns only raw rows and cannot create database-authority provenance. It retains no request input, payload, projection, idempotency key, or credential; only raw-result fields allowed by the frozen contract.

## Scanner implementation findings

- Deterministic sorting, normalized slash paths, cycle termination, BOM/comment parsing, current-repository scale, and no source execution: PASS.
- Import resolution covers current relative and `@/` conventions but intentionally rejects nonliteral resolution.
- Cycles with multiple adapter files can emit semantically duplicate findings rooted at each adapter module. This is deterministic but noisy.
- Several new adversarial fixtures assert rule and nonzero status but not all required root/source/chain/location text. Rule-ID coverage therefore overstates scenario-level diagnostic coverage.

## Residual bypass classification

| Pattern | Classification | Reason |
| --- | --- | --- |
| Nonliteral/concatenated dynamic imports | `ACCEPTED_STATIC_LIMITATION` | Explicitly documented and not safely resolvable without execution. |
| Generic capability alias (`const run = client.query`) | `NEW_FINDING` | Statically knowable and explicitly required; currently passes. |
| Constant/enum-derived computed generic key | `NEW_FINDING` | Locally resolvable constant propagation is absent. |
| Symbol-only members | `ACCEPTED_STATIC_LIMITATION` | Not a string-named generic surface unless aliased to a forbidden string. |
| Object spread from a generic surface | `NEW_FINDING` | Can expose the forbidden capability under current adapter code without detection. |
| Mapped/conditional type yielding a forbidden key | `NEW_FINDING` | Statically constrained variants are not inspected. |
| Namespace package import | `STATICALLY_COVERED` | Forbidden package specifier itself is detected. |
| `.d.ts` and JS under scan roots | `STATICALLY_COVERED` | Extension traversal and TypeScript parser include them. |
| Generated files outside scan roots | `ACCEPTED_STATIC_LIMITATION` | Build/review policy must constrain generation. |
| Runtime monkey-patching/evaluation | `ACCEPTED_STATIC_LIMITATION` | Not statically provable; factory itself remains frozen. |

## Frozen-contract compatibility

PASS. The exact eight fields, one-operation port, raw acquisition result, provenance, idempotency, replay, safe errors, outbox, lifecycle declarations, disabled factory, no fallback, no dual write, and no activation are unchanged.

## Documentation accuracy

The ADR, skeleton, and blockers remain accurate. The boundary report correctly documents the absolute-static-proof limitation. The A2.4A.2 report overstates generic alias coverage at `runtime-integration/A2.4A.2-BOUNDARY-HARDENING-REPORT.md:10`; the implementation does not track aliases derived from `client.query` or equivalent surfaces.

## Independent validation

- `npx tsc --noEmit`: PASS.
- ESLint: PASS, zero errors and six unrelated pre-existing warnings.
- Core V2 contract runner: PASS, 193 tests.
- Scanner fixtures: PASS, 57 fixtures.
- Stable rule IDs: 19; fixture-covered IDs: 19; live-only: zero; intentionally non-isolatable: zero; uncovered: zero.
- Adapter type tests: PASS through TypeScript.
- Live scanner: PASS.
- Next.js production build: PASS, 114 pages.
- `git diff --check` and strict trailing-whitespace scan: PASS.

Passing fixtures/live scan do not negate the independently reproduced zero-finding generic alias case.

## Findings matrix

| Severity | Path and line | Issue | Bypass/failure scenario | Violated contract | Required correction | Commit impact |
| --- | --- | --- | --- | --- | --- | --- |
| HIGH | `scripts/check-core-v2-boundaries.mjs:276-310` | Generic detector checks declared names but does not propagate generic capabilities through aliases/spreads/derived static keys. | `const run = client.query; export { run }` passes; an adapter gains arbitrary query behavior under an innocuous name. | Generic API aliases must be rejected; transport must remain one-operation only. | Add deterministic value/symbol provenance for property-access aliases and statically resolvable computed/mapped forms, with adversarial and safe fixtures. | Blocks commit. |
| MEDIUM | `scripts/check-core-v2-boundaries.mjs:548-554` | Package-chain diagnostic omits forbidden source line/column and anchors structured location at the root. | Reviewer cannot directly locate the external import in a multi-hop wrapper from the emitted finding alone. | Exact source location required. | Include source line/column in description or structured related location while retaining root location/chain. | Correct before commit with HIGH fix. |
| MEDIUM | `scripts/check-core-v2-contracts.mjs:1278-1404` | New scenario fixtures do not uniformly assert exact root, source, chain, and source location. | Diagnostics regress while fixture count and rule-ID coverage remain green. | Required adversarial fixture assertions. | Add scenario-level required-text assertions, including safe zero-finding controls. | Correct before commit with HIGH fix. |
| LOW | `scripts/check-core-v2-boundaries.mjs:545-590` | Multiple adapter roots in a cycle can emit duplicate semantic findings. | Noisy CI output obscures the primary chain, though ordering is deterministic. | Scanner quality/duplicate review. | Deduplicate on rule/root/source/location or document intentional per-root findings. | Nonblocking alone. |
| LOW | `runtime-integration/A2.4A.2-BOUNDARY-HARDENING-REPORT.md:10` | Claims alias coverage broader than implementation. | Reviewers infer the reproduced generic alias bypass is closed. | Accurate documentation. | Narrow claim until corrected and proven. | Must update with correction. |
| INFORMATIONAL | `lib/core-v2/adapter/internal/test-fake-transport.ts:17-122` | Fake snapshot isolation works for the frozen raw-row shapes reviewed. | No reproduced mutation leak. | Deterministic fake. | None. | No impact. |

## Commit readiness

`CORRECTION_REQUIRED`. The unresolved HIGH finding prevents commit. Package installation, concrete transport, credentials, database access, P2D.20 execution, route integration, and activation remain prohibited.

## SQL/database/package proof

No SQL was created or executed, no database/provider was contacted, no package was installed, no application/package/environment file was changed, and no staging, commit, or push occurred.

## Exact next phase

Perform A2.4A.4 as a narrow scanner-provenance correction: cover generic property aliases and statically resolvable derived surfaces, add exact diagnostic assertions, include package source coordinates, reconcile duplicate semantics, and correct the hardening report. Then repeat independent review before commit.

A24A3_900_INDEPENDENT_POST_HARDENING_REVIEW_COMPLETE
