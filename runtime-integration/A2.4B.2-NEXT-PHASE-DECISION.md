# A2.4B.2 Next Phase Decision

Status: ADOPT_DIRECT_EXECUTE_LOGIN_MODEL

## Executive decision

Architecture B replaces architecture A for the application runtime identity path.

It preserves or improves least privilege by reducing the application graph to one exact EXECUTE edge, eliminates dependency on undocumented Supavisor role-state semantics, retains the frozen P2D.20 SECURITY DEFINER boundary, keeps `service_role` rejected, and introduces no direct object access.

## `afex_core_runtime` decision

`DEPRECATE_FROM_APPLICATION_RUNTIME_PATH`

The role remains untouched in this documentation phase. The application receives no membership. Any internal/worker use and the disposition of its existing P2D.20 grant require independent review.

## Superseded work

The A2.4B.1 role-switch disposable test environment and proposed role-switch test-package phase must not proceed. Historical documents remain unchanged and are interpreted through this amendment.

## Remaining blockers

- No forward, attestation, rollback, revocation, or cleanup SQL design exists for direct EXECUTE.
- No dependency or harness implementation is authorized.
- LOGIN attributes, exact/effective privileges, function definition, and revocation are not attested.
- Transaction pooling, advisory locks, cancellation, unknown outcomes, quarantine, capacity, TLS, secrets, denials, and observability are untested.
- No credential, database connection, P2D.20 call, route integration, deployment, or activation is authorized.

## Exact next phase

Proceed to documentation/design only:

**A2.4B.3 — Direct-EXECUTE SQL Package and Isolated Harness Design**

That phase may design forward SQL, read-only attestation SQL, rollback/revocation SQL, a fixed-operation isolated harness, operator runbook, evidence contract, and hash manifest. It must not execute SQL, contact a database/provider, install dependencies, provision credentials, call P2D.20, modify application routes, or activate Core V2 without later explicit approval.
