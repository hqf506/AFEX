# A2.4D External Gate Inventory

Status: AUTHORITY AND OPERATOR INPUT COLLECTION ONLY; EXECUTION UNAUTHORIZED

| Gate | Classification | Current evidence | Closure evidence |
|---|---|---|---|
| Supabase custom password-bearing LOGIN support | REQUIRES_PROVIDER_CONFIRMATION | PostgreSQL syntax and the disposable design exist; hosted-project support is not proven | Applicable Supabase answer classified by the requested response vocabulary |
| `CREATE ROLE` / `ALTER ROLE` authority | REQUIRES_PROVIDER_CONFIRMATION | Prior read-only design separates catalog capability from managed-provider permission | Supported identity and exact execution channel |
| Role password support | REQUIRES_PROVIDER_CONFIRMATION | No hosted-project approval | Supported password lifecycle without disclosed values |
| Finite `VALID UNTIL` support | REQUIRES_PROVIDER_CONFIRMATION | Required by design only | Provider confirmation applicable to the project/tier |
| Finite connection limit support | REQUIRES_PROVIDER_CONFIRMATION | Required by design only | Provider confirmation plus approved numeric operator input |
| Ownership assignment authority | REQUIRES_PROVIDER_CONFIRMATION | PostgreSQL ownership semantics are known; managed authority is unresolved | Supported owner-transfer channel and identity |
| Disposable owner strategy | REQUIRES_OPERATOR_INPUT | Dedicated owner is preferred; fallback owner is not approved | Security/database-owner decision informed by provider response |
| `afex_function_owner` suitability | REQUIRES_OPERATOR_INPUT | Role exists in the frozen design; disposable-test blast radius is undecided | Named security/database approver decision |
| Schema/function creation authority | REQUIRES_PROVIDER_CONFIRMATION | No harmless probe creation is authorized | Provider-supported channel plus authority proof |
| Exact SQL execution channel | REQUIRES_OPERATOR_INPUT | Dashboard editor versus direct `psql` is unresolved | Named channel, connected identity, operator, and runbook approval |
| Dashboard SQL editor versus direct `psql` authority | REQUIRES_PROVIDER_CONFIRMATION | Authority equivalence must not be inferred | Provider statement for each channel |
| Durable LOGIN disablement | REQUIRES_PROVIDER_CONFIRMATION | NOLOGIN/password/revoke sequence is designed, not hosted-approved | Supported disable procedure and verification |
| Active-session termination | REQUIRES_PROVIDER_CONFIRMATION | No authority or pooler behavior is proven | Supported scoped termination identity/procedure |
| Exact EXECUTE revocation | PROVEN | PostgreSQL exact-signature ACL model and direct-EXECUTE architecture are frozen | Runtime attestation still required after any future setup |
| Cleanup and dependency handling | BLOCKED | Cleanup source exists but provider authority and dependency approval do not | Provider support, named cleanup owner, approved dependency process, absence attestation |
| Transaction Pooler endpoint class | PROVIDER_CONFIRMED | Supabase Transaction Pooler is the provider-recommended serverless topology | Project-specific endpoint still requires operator-supplied masked suffix |
| Transaction Pooler port | REQUIRES_PROVIDER_CONFIRMATION | Generic port 6543 guidance exists; project applicability is not proven | Project/tier-specific confirmation |
| TLS mode | REQUIRES_PROVIDER_CONFIRMATION | Encryption guidance does not prove required mode | Exact accepted `sslmode` and STOP behavior |
| Certificate/hostname validation | REQUIRES_PROVIDER_CONFIRMATION | No CA or hostname contract is retained | Provider-supported CA and hostname-verification contract |
| IPv4/IPv6 requirements | REQUIRES_OPERATOR_INPUT | Depends on selected execution host and project endpoint | Masked host/network topology decision |
| Connection capacity | REQUIRES_PROVIDER_CONFIRMATION | Formula exists; project/tier limits are absent | Pooler/database limits and current safe headroom |
| Safe pool-size budget | REQUIRES_OPERATOR_INPUT | Vercel recommends bounded process-global pooling and not blindly forcing one | Numeric allocation satisfying provider capacity and operator concurrency |
| Timeout hierarchy | REQUIRES_OPERATOR_INPUT | Ordering contract is proven; numbers are intentionally unset | Positive bounded lock < statement < controller < process values plus transaction/checkout limits |
| Advisory-lock caveats | REQUIRES_RUNTIME_TEST | No provider guarantee establishes transaction-lock behavior through the selected pooler | Provider statement plus two-client isolated proof |
| Cancellation behavior | REQUIRES_RUNTIME_TEST | Vercel lifecycle guidance is not database cleanup proof | Isolated cancellation/quarantine evidence |
| Abrupt-disconnect behavior | REQUIRES_RUNTIME_TEST | Undocumented for the selected path | Isolated disconnect, cleanup, and reuse evidence |
| Unknown commit outcome handling | PROVEN | Frozen design requires same-key reconciliation and never a new key | Runtime proof still required for classification behavior |
| Evidence retention | REQUIRES_OPERATOR_INPUT | Git-ignored local evidence boundary exists; duration/readers/deletion are unset | Named location, readers, duration, protection, deletion owner |
| Cleanup ownership | REQUIRES_OPERATOR_INPUT | No accountable operator is named | Primary/backup owner and escalation authority |
| Secret creation/storage/rotation/revocation | BLOCKED | Credentials are prohibited from Git/evidence; lifecycle owners and store are absent | Named owners, approved secret store/injection, lifetime, rotation and emergency revoke |
| Execution location | REQUIRES_OPERATOR_INPUT | Local versus isolated Vercel remains undecided | Controlled host, identity, network boundary, and local-simulation decision |
| Node version | REQUIRES_OPERATOR_INPUT | Repository target direction is Node 24; exact approved version is absent | Exact supported version and runtime evidence |
| Dependency versions | REQUIRES_DEPENDENCY_APPROVAL | `pg` direction is approved; no version or installation is approved | Security review and exact version/integrity/lockfile decision |
| Vercel versus local execution | REQUIRES_OPERATOR_INPUT | Vercel supports outbound PostgreSQL and lifecycle integration; local test remains possible | One selected isolated execution topology |

No `PROVEN`, `PROVIDER_CONFIRMED`, or `OPERATOR_CONFIRMED` row independently authorizes SQL, credentials, installation, database contact, UI execution, or Core V2 activation.
