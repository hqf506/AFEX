# A2.4A.4 Generic Capability Provenance Hardening

Status: IMPLEMENTED — READY FOR INDEPENDENT REVIEW

## Resolution summary

- Generic database capabilities are tracked from their original forbidden member through variable and multi-hop aliases, exports, destructuring, function declarations, function expressions, arrow functions, object/class methods, function-valued properties, CommonJS assignments, local wrappers, and statically reachable module chains. Function and method forms were completed by A2.4A.6.
- Static computed keys resolve literal consts and alias chains, enum string members, readonly object/tuple values, static templates, and literal concatenation.
- Type analysis covers direct object surfaces, intersections, mapped keys, `keyof` copied keys, declaration merging/inheritance, and concretely instantiated boolean conditional types. Fully generic uninstantiated conditionals remain allowed until a forbidden concrete surface is provable.
- Package diagnostics now contain adapter root, complete chain, package, wrapper/source module, exact source line/column, and import kind.
- Generic diagnostics contain original member coordinates, coordinate-bearing provenance steps, and final exposure/use coordinates.
- Identical reports are deduplicated deterministically. Cycle root selection prevents equivalent per-module reports while preserving distinct violations.
- Adapter fixture assertions validate rule, nonzero/zero status, formatted root coordinates, and rule-specific diagnostic structure. New adversarial and safe fixtures cover the approved matrix.

## Preserved boundaries

The P2D.20 eight-field input, raw result, one-operation transport, authority provenance, idempotency, replay, safe-error, outbox, lifecycle, disabled factory, fake allowlist, and no-activation posture are unchanged. No package, SQL, credential, connection, concrete transport, route, fallback, dual write, or feature activation was introduced.

## Accepted static limitations

Nonliteral runtime-generated imports, arbitrary runtime-computed property names, malicious monkey-patching, `eval`/`new Function`/generated code, symbol-only runtime members, and unchecked `any`/`unknown` casts that erase all static information cannot be absolutely proven by this scanner. They remain prohibited by review and build policy.

## Validation contract

The completed validation produced these results:

- TypeScript: PASS (`npx tsc --noEmit`).
- ESLint: PASS with zero errors and six unrelated pre-existing warnings.
- Core V2 contracts: PASS, 295 tests after A2.4A.10.
- Scanner fixtures: PASS, 158 fixtures after A2.4A.10, including transparent/branch expressions, tuple spreads, class expressions/aliases, effective overrides, and all earlier propagation forms.
- Stable scanner rules: 19; isolated coverage: 19; uncovered: zero.
- Adapter type tests: PASS through TypeScript and the contract runner.
- Live boundary scan: PASS.
- Next.js production build: PASS, 114 pages.
- `git diff --check`: PASS.
- Strict trailing-whitespace scan: PASS.

## Findings-resolution matrix

| Finding | Resolution |
| --- | --- |
| HIGH generic capability aliases | Provenance follows direct, multi-hop, destructured, returned, wrapped, exported, and CommonJS-assigned values. |
| HIGH derived surfaces | Static computed keys, resolvable spreads, mapped types, and concrete conditional types are inspected. |
| MEDIUM package diagnostic coordinates | Root, chain, source, package, import kind, line, and column are emitted. |
| MEDIUM incomplete fixture assertions | Every failing adapter fixture validates common coordinates plus its rule-specific diagnostic contract; safe fixtures validate zero targeted and unrelated findings. |
| LOW cyclic duplicates | Deterministic root selection and exact finding-key deduplication collapse equivalent reports. |
| LOW documentation overstatement | A2.4A.2 and the boundary report now state enumerated static coverage and explicit limitations. |

## No-activation verification

Application adapter imports, route/server-action adapter imports, adapter environment reads, forbidden driver imports in the adapter, executable P2D.20 calls, and `Pool`/`Client` construction are all zero. Package changes and SQL changes are zero. No application, route, feature flag, or legacy order path changed.

A24A4_900_GENERIC_CAPABILITY_PROVENANCE_HARDENING_COMPLETE
