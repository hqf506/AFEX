# A2.4A.8 Getter, Indexed Callable, and Class Provenance Correction

Status: IMPLEMENTED — READY FOR FINAL INDEPENDENT REREVIEW

## Resolution summary

The four A2.4A.7 HIGH findings are corrected. Capability provenance now covers object, nested, instance, static, and inherited getters; statically computed method/getter/property declarations; statically indexed and destructured arrays/readonly tuples, including nested and returned containers; and static, detached, one-level, and multi-level inherited class members. A2.4A.10 completes transparent/branch expressions, tuple spreads, class expressions/aliases, and effective override resolution.

Computed declaration names reuse the bounded static evaluator for literals, const aliases, enums, readonly object/tuple values, static templates, and compile-time concatenation. Runtime-generated names remain unresolved by design.

Class provenance distinguishes static from instance members, carries safe-to-resolve heritage across levels, respects declared overrides, copies getter/function-valued properties, and records inheritance and instance steps in diagnostics. Exported object/class/array surfaces are inspected for contained callable or getter capabilities.

## Fixtures and diagnostics

Isolated failing fixtures cover object/class/static/inherited getters, computed object/static methods and getters, literal/const/readonly/nested/returned indexed callables, destructuring, detached/static/inherited methods, multilevel inheritance, and barrel/CommonJS/cycle propagation. Safe fixtures cover harmless getters/inheritance, `acquire` returns, string arrays, acquire tuples, and unresolved dynamic names.

Diagnostics retain the adapter root, original member, declaration, computed/index/inheritance provenance, final exposure, exact coordinates, and deterministic ordering under the stable `adapter_generic_query_surface` rule.

## Frozen-contract and activation status

No adapter, application, route, package, environment, SQL, migration, production configuration, P2D.20 mapping, acquire-only transport, raw result, authority/idempotency/replay/error/outbox/lifecycle contract, factory, fake allowlist, fallback, dual-write, or activation behavior changed.

## Validation contract

- Core V2 contracts: 295 tests after A2.4A.10.
- Scanner fixtures: 158 after A2.4A.10.
- Stable rules: 19.
- Isolated coverage: 19.
- Uncovered rules: zero.
- TypeScript: PASS.
- ESLint: PASS with zero errors and six unrelated pre-existing warnings.
- Live boundary scan: PASS.
- Production build: PASS, 114 pages.
- `git diff --check`: PASS.
- Strict trailing-whitespace scan: PASS.
- Application/route adapter imports, adapter environment reads, executable P2D.20 calls, Pool/Client construction, package changes, SQL changes, feature activation, and legacy-path changes: zero.

## Remaining static limitations

Runtime-generated names/indexes, dynamic prototype mutation, monkey-patching, `eval`/`new Function`, generated code, symbol-only members, nonliteral runtime imports, and information-erasing `any`/`unknown` casts remain outside absolute static proof and prohibited by review policy.

A24A8_900_GETTER_INDEXED_CALLABLE_AND_CLASS_PROVENANCE_CORRECTED
