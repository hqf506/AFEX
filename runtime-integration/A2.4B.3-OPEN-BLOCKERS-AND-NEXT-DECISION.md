# A2.4B.3 Open Blockers and Next Decision

Status: A2.4C.10 POWERSHELL 5.1 JSON COMPATIBILITY CORRECTION APPLIED; INDEPENDENT REREVIEW REQUIRED

## Current distinctions

- Source-ready pending rereview: expanded attestation, hardened runner, semantic harness core, controller contracts, evidence schema, runbook, and manifest.
- Provider-blocked: custom LOGIN and owner authority, disable/revoke, active-session termination, cleanup, exact pooler/TLS facts, and capacity.
- Operator-blocked: execution location, limits, secret lifecycle, evidence retention, and accountable owners.
- Dependency-blocked: approved Node/npm/`pg` versions and later installation authority.
- Runtime-unproven: all database, pooler, identity, lock, cancellation, disconnect, exhaustion, isolation, quarantine, and cleanup behavior.
- Execution-not-authorized: SQL, runner, harness, credential provisioning, package installation, provider contact, and Core V2 activation.

## Decision

A2.4C.7 approved the major result-integrity and unified-redaction controls, then identified missing named negative fixtures, missing runner outcome fixtures, and a fixed schema/tool validator bypass. A2.4C.8 adds only that bounded coverage and routes the fixed schema/tool versions through the shared validator.

A2.4C.9 found that PowerShell 5.1 fixture indentation did not match the operational two-space evidence layout. A2.4C.10 removes indentation, newline, and ordering dependence from structural validation while retaining explicit fail-closed duplicate-key and type-shape checks.

The exact next phase is `A2.4C.11 — Final Independent PowerShell JSON Compatibility Approval Rereview`. It must remain read-only and must not execute the package.
