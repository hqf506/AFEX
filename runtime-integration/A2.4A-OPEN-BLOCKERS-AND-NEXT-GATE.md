# A2.4A Open Blockers and Next Gate

The skeleton is reviewable but permanently disabled. Supabase confirmation remains required for explicit-transaction backend pinning, `SET ROLE`/`SET LOCAL ROLE`, membership administration, cleanup after commit/rollback/cancel/disconnect, and transaction advisory locks. Project evidence remains required for connection capacity, TLS/network behavior, Node 24 driver behavior, prepared-statement disabling, cancellation/unknown outcomes, and observability.

The next phase is an external-evidence reconciliation and isolated-test authorization gate. Only after that gate may a separate change request authorize dependencies and a credential-free concrete-transport test harness. Production credentials, application routes, P2D.20 invocation, and activation remain prohibited.
