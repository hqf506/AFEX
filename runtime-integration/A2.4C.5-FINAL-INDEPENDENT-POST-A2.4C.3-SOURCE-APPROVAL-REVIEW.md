# A2.4C.5 Final Independent Post-A2.4C.3 Source Approval Review

Decision: `NARROW_SOURCE_CORRECTIONS_REQUIRED`

## Executive verdict

The current source does not contain corrections for the three HIGH findings recorded by A2.4C.4. The context statement that A2.4C.3 subsequently corrected those findings is chronologically and materially inconsistent with the repository: A2.4C.3 predates A2.4C.4, and the cited code remains unchanged. No major redesign is required, but external gate collection is not approved.

## Repository preconditions

- Branch `master`; HEAD and `origin/master` are `4fd239784ea6d38e1744b2b62d664aa33b02496c`.
- Ahead/behind `0/0`; staged files `0`; no active Git operation.
- Visible paths legitimately extend through A2.4C.4; no unrelated path exists.
- `git diff --check` passes.

## A2.4C.4 finding resolution

| Finding | Independent result |
|---|---|
| HIGH 1: validate retained results despite nonzero harness exit | NOT RESOLVED |
| HIGH 2: route every serialized projection through the allowlist | NOT RESOLVED |
| HIGH 3: strictly validate all serialized version evidence | NOT RESOLVED |
| MEDIUM 1: A2.4C.2 traceability artifact | NOT RESOLVED; absence is accurately recorded by A2.4C.4 |
| MEDIUM 2: readiness overstatement | NOT RESOLVED; A2.4C.3 still claims `TARGETED_CORRECTIONS_COMPLETE` |

## Runner result contract

Decision: `RUNNER_RESULT_CONTRACT_CORRECTION_REQUIRED`.

At runner line 198, any nonzero process exit throws before result-file existence, digest recomputation, JSON parsing, schema/run/marker checks, and final-status validation at lines 199-232. Because the harness deliberately exits nonzero for `INCOMPLETE`, expected incomplete evidence is never integrity-validated. Timeout and launch failures remain correctly fail-closed, but a safely retained result must be validated before applying the process-exit verdict.

## Evidence redaction

Decision: `EVIDENCE_REDACTION_CORRECTION_REQUIRED`.

The authoritative allowlist function at harness lines 107-113 is applied to `results` at line 378 only. `identityAndPidEvidence`, `contaminationMatrix`, `lockEvidence`, and `cancellationTimeoutDisconnectEvidence` at lines 369 and 371-373 directly serialize unsanitized `CaseResult` objects. These duplicate projections bypass the claimed single redaction path.

## Version evidence

Decision: `VERSION_EVIDENCE_CORRECTION_REQUIRED`.

Harness lines 158-165 accept arbitrary nonempty npm and pg environment strings and serialize them verbatim. No bounded length, single-line rule, character grammar, or normalization exists. Node is sourced from `process.version`, but its serialized form is not checked against an evidence grammar. Optional `@types/pg` and `@vercel/functions` values are neither validated nor explicitly represented as absent. Arbitrary environment payload serialization therefore remains possible.

## Regression review

No regression was found in the read-only attestation, source-hash pinning, JSON validation code itself, bounded termination, exact assertion identifiers, denial SQLSTATE fields, mandatory final-status binding, timeout hierarchy, partial-setup runbook, hard-disabled gates, or manifest synchronization. The defect is control-flow reachability of result validation plus incomplete redaction/version validation, not loss of those other controls.

## Manifest

- Rows: `12`.
- Count/byte/SHA-256 mismatches: `0`.
- Missing/stale rows: `0` for the A2.4B.3 execution package.
- Self-hash treatment: explicit and non-recursive.
- Manifest SHA-256: `458d98709e44dc8b17ff7c872844af543b5d6c9beeee174f81f71763b7608a5f`.

## Findings matrix

| Severity | Exact path and line | Issue | Failure scenario | Violated contract | Required correction | Execution impact | Commit impact |
|---|---|---|---|---|---|---|---|
| HIGH | `database-reconciliation/core-v2/P2D/A2.4B.3-RUN-ISOLATED-DIRECT-EXECUTE-TESTS.ps1:198-232` | Process exit is evaluated before retained-result integrity. | Expected `INCOMPLETE` or failed evidence can be malformed or digest-mismatched without runner validation. | Integrity validation whenever a safe results artifact exists, regardless of harness exit. | Validate any existing result pair first; then apply nonzero exit/final-status failure. Missing files remain fail-closed. | Blocks source approval. | Runner, manifest, contract/status documents. |
| HIGH | `database-reconciliation/core-v2/P2D/A2.4B.3-ISOLATED-DIRECT-EXECUTE-HARNESS.ts:107-113,369-373,378` | Duplicate result projections bypass the allowlist. | A future observation field is omitted from `results` but leaks through a duplicate matrix. | One authoritative deterministic serialization path. | Sanitize once and derive every projection from sanitized results, or sanitize every projection identically. | Blocks redaction approval. | Harness and manifest. |
| HIGH | `database-reconciliation/core-v2/P2D/A2.4B.3-ISOLATED-DIRECT-EXECUTE-HARNESS.ts:158-165,364-366` | Serialized runtime/dependency versions lack strict bounded validation and explicit optional states. | Multiline, excessive, URL, token, or arbitrary environment text enters evidence. | No arbitrary environment payload; bounded deterministic version evidence. | Enforce exact version grammar/length/normalization for Node, npm, pg, and optional packages; represent missing optional packages explicitly. | Blocks version-evidence approval. | Harness, manifest, evidence contract. |
| MEDIUM | `runtime-integration/A2.4C.2-INDEPENDENT-POST-CORRECTION-EXECUTION-READINESS-REREVIEW.md` (absent) | Repository-local A2.4C.2 traceability remains missing. | Review history depends on later summaries and conversation state. | Complete review provenance. | Materialize only the approved historical review if governance requires it; do not fabricate or revise findings. | No runtime authority impact. | Documentation only. |
| MEDIUM | `runtime-integration/A2.4C.3-TARGETED-POST-REREVIEW-SOURCE-CORRECTION.md:3-15` | Source readiness is overstated. | External-gate work may begin despite unresolved HIGH findings. | Current status accuracy. | Mark the claim historical/unapproved and reference this review and the next correction phase. | Blocks status approval. | Status documents only. |

## Static validation

- Windows PowerShell 5.1 parser errors: `0`.
- Node TypeScript syntax: PASS.
- `npx tsc --noEmit`: PASS.
- SQL remains read-only by static classification; no mutation was introduced.
- Credential/full-URL literal scan: no finding.
- Manifest rows/mismatches: `12/0`.
- UTF-8 without BOM, LF endings, trailing-whitespace scan: PASS.
- `git diff --check`: PASS; staged files `0`.

## Boundaries and next phase

No SQL, runner, or harness was executed; no database/provider was contacted; no package was installed; no credential was added; no Git write occurred; Core V2 remains hard-disabled. Provider authority, operator values, dependency approval, runtime evidence, cleanup/absence proof, and execution authorization remain external blockers.

Exact next phase: `A2.4C.6 — Result-Integrity, Unified Redaction, and Version-Evidence Correction`, restricted to the three HIGH findings and truthful status synchronization, followed by independent rereview.
