# A2.3H.2A Remaining Supabase Questions

The Vercel response closes no Supavisor or PostgreSQL role-semantic question. Submit only these minimum unresolved questions:

1. In Transaction Pooler mode on port 6543, is one PostgreSQL backend retained for the entire explicit transaction from `BEGIN` through `COMMIT` or `ROLLBACK`?
2. Can a dedicated `NOINHERIT` custom LOGIN execute `SET ROLE` to its SET-only custom `NOLOGIN` role inside that transaction?
3. Is `SET LOCAL ROLE` supported, and what exact reset behavior occurs at transaction end?
4. Before backend reassignment, is role/session state guaranteed to be cleared after:
   - successful commit;
   - explicit rollback;
   - statement cancellation or timeout;
   - abrupt client disconnect?
5. Are transaction-level advisory locks supported with PostgreSQL transaction-end release semantics through this pooler mode?
6. Please confirm that persistent prepared statements are unsupported or unsuitable in Transaction Pooler mode and identify any client configuration requirement.

For each answer, request a distinction between guaranteed behavior, documented limitation, and behavior requiring isolated runtime proof. No credential, connection string, project secret, project reference unless securely required, or business data should accompany the request.

## Gate mapping

- Question 1 → G3
- Questions 2–3 → G4 and G2 where membership support is clarified
- Question 4 → G5 and G10
- Question 5 → G11
- Question 6 → G7 and G11

Positive answers do not replace the mandatory isolated runtime tests.
