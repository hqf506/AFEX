# A2.4D Dependency Approval Proposal

Decision: `MORE_DEPENDENCY_EVIDENCE_REQUIRED`

| Dependency | Purpose | Current decision | Required review |
|---|---|---|---|
| `pg` | Fixed-operation PostgreSQL client and bounded pool for the isolated harness/future trusted adapter | Required in principle; version unset; installation unauthorized | Node 24 compatibility, maintained release, advisories, transitive tree, integrity/provenance, transaction-pool behavior, cancellation API, unnamed-query behavior and lockfile diff |
| `@types/pg` | Compile-time types if the approved `pg` release does not supply sufficient types | Conditional; version unset | Compatibility with exact `pg` and TypeScript versions; omit if unnecessary |
| `@vercel/functions` | `attachDatabasePool` lifecycle integration if execution occurs on Vercel | Conditional; version unset | Vercel runtime compatibility, package provenance/advisories and necessity for selected execution location |

## Approval sequence

1. Freeze execution location and exact Node version.
2. Obtain current authoritative package metadata and security advisories without changing the repository.
3. Select exact versions through dependency/security review; do not infer versions from examples.
4. Approve expected `package.json` and lockfile impact separately.
5. Install only under explicit later authorization, review the exact lockfile graph, then rerun static/type/build checks.

No dependency version is invented or approved here. No package was installed and no package or lockfile was modified.
