# A2.4A.6 Function and Method Provenance Correction

Status: IMPLEMENTED — READY FOR FINAL INDEPENDENT REREVIEW

## Correction summary

The A2.4A.5 HIGH bypass is resolved. Statically resolvable forbidden capability returns now propagate through function declarations, function expressions, named function expressions, expression/block arrow functions, object and nested-object methods, function-valued object properties, class methods on statically constructed instances, and namespace-assigned functions. A2.4A.8 completes getter, computed declaration, indexed callable, and static/inherited class coverage.

Callable provenance follows function, method, property, and destructured aliases; multi-hop aliases; calls; named/default/CommonJS exports; local barrels; literal local import graphs; and cycles. Diagnostics retain the original forbidden member, declaration, alias/call steps, final exposure, exact coordinates, and adapter-root import chain.

## Safety and false-positive controls

String-returning functions, harmless object methods, `acquire`-returning functions, `queryLabel`/`queryMetadata`, comments, and documentation strings remain allowed. No runtime evaluation is attempted.

## Frozen-contract preservation

The eight-field P2D.20 mapping, one-operation transport, raw result, authority provenance, idempotency, replay, safe errors, outbox, lifecycle semantics, disabled factory, fake allowlist, no-service-role fallback, no-dual-write rule, and no-activation posture are unchanged. No adapter implementation, application code, package, SQL, environment, route, migration, or production configuration changed.

## Fixture coverage

New isolated failing fixtures cover function expressions, named function expressions, both arrow forms, object/nested methods, function/arrow-valued properties, destructured methods, multi-hop method aliases, CommonJS exports, barrel and cycle propagation, class methods, and namespace assignments. Six safe controls cover the corresponding harmless forms.

## Validation contract

- Core V2 contracts: 267 tests after A2.4A.8.
- Scanner fixtures: 130 after A2.4A.8.
- Stable rules: 19.
- Isolated stable-rule coverage: 19.
- Uncovered rules: zero.
- TypeScript: PASS.
- ESLint: PASS with zero errors and six unrelated pre-existing warnings.
- Live boundary scan: PASS.
- Production build: PASS, 114 pages.
- `git diff --check`: PASS.
- Strict trailing-whitespace scan: PASS.
- Application/route imports, adapter environment reads, forbidden driver imports, executable P2D.20 calls, Pool/Client construction, package changes, SQL changes, feature activation, and legacy-path changes: zero.

## Remaining static limitations

Runtime monkey-patching, `eval`/`new Function`, arbitrary generated code, fully dynamic property resolution, nonliteral runtime imports, symbol-only members, and information-erasing `any`/`unknown` casts remain outside absolute static proof and prohibited by review policy.

A24A6_900_FUNCTION_AND_METHOD_PROVENANCE_CORRECTED
