# A2.4A Boundary and Test Report

Stable scanner rules cover forbidden driver/platform imports, adapter environment access, generic query surfaces, direct P2D.20 calls, caller-provided role targets, and test-fake production reachability. They traverse statically resolvable local import/re-export/require/literal-dynamic chains, including cycles. Generic capability provenance covers enumerated direct, aliased, exported, destructured, transparent-expression, sequence/conditional/nullish, function/arrow, object/class method and getter, function-valued property, named/anonymous class-expression and class/namespace alias, effective static/inherited class, statically indexed/spread array/tuple, object-spread, static computed-key, mapped-type, and concrete conditional-type forms. Package diagnostics include the root, chain, package, source coordinates, and import kind. Adapter fixture diagnostics are structurally asserted, cycles are deterministically deduplicated, and the live repository scan is empty.

Type tests prove the transport aliases the frozen input and raw result, exposes exactly eight input keys, and has only `acquire`. Runtime tests prove stable disabled failure, lifecycle completeness, immutable caller-independent fake snapshots, deterministic failure injection, and the exact test-fake import allowlist.

The skeleton contains `server-only` guards following the checked-in Next.js 16.2.10 guidance. Validation includes TypeScript, ESLint, contract tests and fixture coverage, live boundary scan, production build, whitespace checks, and change inventory.

Current-tree no-activation evidence is separate from future-change enforcement: there are zero application/route/UI imports, environment reads, network/database imports, concrete Pool/Client creations, direct P2D.20 calls, package changes, flags, or legacy-path changes. The scanner provides strong static coverage for the enumerated resolvable forms; runtime-generated/nonliteral module specifiers and arbitrary code generation remain outside absolute static proof and are prohibited by review policy.
