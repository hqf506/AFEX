# A2.4C.4 Final Independent Targeted-Correction Rereview

Decision: `NARROW_SOURCE_CORRECTIONS_REQUIRED`

## Executive verdict

The attestation, early-evidence behavior, termination handling, timeout hierarchy, partial-setup procedure, and manifest pass independent static rereview. The package cannot leave the source-correction loop because the runner skips result integrity validation on every nonzero harness exit, while the harness is currently designed to exit nonzero as `INCOMPLETE`. Evidence redaction also bypasses its allowlist for duplicated result projections and serializes unvalidated dependency-version environment values. No major redesign is required.

## Repository preconditions

- Branch `master`; HEAD and `origin/master` are `4fd239784ea6d38e1744b2b62d664aa33b02496c`.
- Ahead/behind `0/0`; staged files `0`; no active Git operation.
- Visible paths are confined to A2.4B.3 through A2.4C.3; no unrelated path exists.
- `git diff --check` passes.

## Component decisions

| Component | Decision |
|---|---|
| Attestation | `ATTESTATION_FINAL_APPROVED` |
| Early evidence | `EARLY_EVIDENCE_APPROVED` |
| Digest and JSON contract | `RESULT_CONTRACT_NARROW_CORRECTION_REQUIRED` |
| Process termination | `TERMINATION_SOURCE_APPROVED` |
| Redaction/assertion evidence | `REDACTION_AND_ASSERTION_EVIDENCE_CORRECTION_REQUIRED` |
| Denial/final-status binding | `EVIDENCE_FINAL_STATUS_BINDING_APPROVED` |
| Timeout hierarchy | `TIMEOUT_HIERARCHY_APPROVED` |
| Partial-setup runbook | `PARTIAL_SETUP_RUNBOOK_APPROVED` |
| Manifest | `MANIFEST_FINAL_APPROVED` |

## Attestation findings

PostgreSQL 17.6 catalog usage, exact function/overload identity, five ordered TABLE outputs, names, types, modes, zero inputs, complete NULL column-ACL state, exact owner, owner implicit EXECUTE, ACL/effective privilege closure, memberships, ownership denial, object separation, read-only transaction, rollback, and completion marker are source-correct. No business relation or function is executed.

## Runner findings

Repository/script proof, safe-root containment, Git-ignore proof, deterministic no-safe-path marker, safe-path summaries, bounded timeout, checked `taskkill.exe`, bounded post-kill wait, and environment restoration are correct. However, line 198 throws on nonzero harness exit before lines 206-232 verify the evidence digest and JSON contract. The harness intentionally exits `3` for `INCOMPLETE`, so precisely the evidence most requiring review is never integrity-validated by the runner.

## Redaction and evidence findings

Failed assertions preserve safe IDs, phases, expected/actual categories, messages, and timestamps. Denial records contain expected/actual SQLSTATEs and fail on missing or unexpected states. The explicit allowlist is applied only to `results` at line 378; duplicate projections at lines 369 and 371-373 serialize raw `CaseResult` objects without passing through that allowlist. In addition, arbitrary nonempty environment strings accepted as npm/pg versions at lines 158-165 are serialized without a bounded version grammar.

## Timeout and final-status findings

Lock, statement, controller, and process timeouts are positive, bounded, operator-supplied, and strictly ordered. Advisory contention accepts only `55P03`; statement cancellation remains distinct. Mandatory hashes are non-null and syntactically validated. Cleanup, absence attestation, independent review, and unavailable controllers correctly prevent PASS.

## Runbook and manifest

The runbook includes all five partial-setup classifications, immediate STOP, evidence retention, credential/session/cleanup owners, provider escalation, no invented authority, and prohibition on harness execution before reconciliation. The manifest contains 12 rows with zero count, byte, or SHA-256 mismatches. Its self-hash treatment is explicit and non-recursive.

## Findings matrix

| Severity | Exact path and line | Issue | Failure scenario | Violated contract | Required correction | Execution impact | Commit impact |
|---|---|---|---|---|---|---|---|
| HIGH | `database-reconciliation/core-v2/P2D/A2.4B.3-RUN-ISOLATED-DIRECT-EXECUTE-TESTS.ps1:198-232` | Nonzero exit is handled before digest and JSON validation. | An `INCOMPLETE` or failed harness writes truncated, malformed, or digest-mismatched evidence; the runner stops at exit code and never classifies evidence integrity. | Every retained result must receive independent digest/schema/run/marker validation. | If result files exist, validate their digest and JSON contract before applying the nonzero-exit failure; absence remains fail-closed. | Blocks source approval. | Runner, manifest, evidence contract, status report. |
| HIGH | `database-reconciliation/core-v2/P2D/A2.4B.3-ISOLATED-DIRECT-EXECUTE-HARNESS.ts:107-113,369-373,378` | The allowlist is not applied to every serialized result projection. | A future fixed result adds an unapproved observation; duplicate identity/contamination/lock/timeout projections serialize it despite `results` redaction. | Only explicitly approved evidence fields may be serialized. | Build all projections from one sanitized result set, or apply the same allowlist to each projection before serialization. | Blocks redaction approval. | Harness and manifest. |
| HIGH | `database-reconciliation/core-v2/P2D/A2.4B.3-ISOLATED-DIRECT-EXECUTE-HARNESS.ts:158-165` | Dependency-version environment values are only checked for presence and are serialized verbatim. | An accidental URL, token, or arbitrary value in a version variable is written to evidence. | No credential/arbitrary environment value may be serialized; diagnostics must be bounded. | Validate exact bounded version syntax and length before serialization, or record only a safe digest/approved identifier. | Blocks redaction approval. | Harness and manifest. |
| MEDIUM | `runtime-integration/A2.4C.2-INDEPENDENT-POST-CORRECTION-EXECUTION-READINESS-REREVIEW.md` (missing) | The required A2.4C.2 review artifact is absent. | Historical traceability relies only on conversation output and later summaries. | Final review coverage and status provenance. | Materialize the approved A2.4C.2 review without altering its findings, if governance requires repository-local history. | Does not independently authorize or block runtime, but prevents complete document traceability. | One documentation artifact; no executable source. |
| MEDIUM | `runtime-integration/A2.4C.3-TARGETED-POST-REREVIEW-SOURCE-CORRECTION.md:3-15` | The status claims all targeted corrections complete despite the three source defects above. | Reviewers may treat evidence integrity and redaction as approved. | Current documents must avoid readiness overstatement. | Mark the claim historical/pending and point to the next narrow correction. | Blocks status consistency approval. | Current status documents only. |

## Independent static validation

- Windows PowerShell 5.1 parser errors: `0`.
- Node TypeScript syntax: PASS.
- `npx tsc --noEmit`: PASS.
- SQL: one read-only BEGIN, one ROLLBACK, zero COMMIT, one completion marker, no mutation token.
- Manifest rows/mismatches: `12/0`.
- Manifest SHA-256: `458d98709e44dc8b17ff7c872844af543b5d6c9beeee174f81f71763b7608a5f`.
- UTF-8 without BOM, LF endings, trailing-whitespace scan: PASS.
- Credential/full-URL literal scan: no finding.
- `git diff --check`: PASS; staged files `0`.

## Boundaries and next phase

No SQL, runner, or harness was executed; no database/provider was contacted; no package was installed; no credential was added; Core V2 remains hard-disabled. Provider authority, operator values, dependency approval, runtime evidence, cleanup/absence proof, and execution authorization remain external blockers rather than source defects.

Exact next phase: `A2.4C.5 — Final Evidence-Integrity and Redaction Corrections`, restricted to the three executable-source findings and status synchronization above, followed by one independent rereview.
