# A2.5 PD6F Transaction-Pool Evidence Execution Plan

## Authority and present boundary

PD6F is the single local transaction-pool scenario `PD6-D-032`. Its evidence tier is `PD6F_POOLER`; its endpoint class is `PGBOUNCER_TRANSACTION_POOL`. PgBouncer is frozen at 1.25.2 in transaction mode and is reachable only at `127.0.0.1:56432`. Its backend route is the dedicated network alias `pd6f-postgres-backend:5432` for database `afex_a25_pd6` as role `afex_a25_pd6_runner`.

The implementation phase authorized only inert imports, fake/unit tests, static inspection, and non-network regressions. One later human execution was separately authorized and accepted through the gates recorded below. This record does not authorize another execution. Provider scenarios `PD6-D-033` and `PD6-D-034`, Supavisor, production services, and Vercel remain deferred.

## Frozen infrastructure authority

- Product/version/mode: `PGBOUNCER` / `1.25.2` / `TRANSACTION`
- Listener authority: TCP only; `unix_socket_dir =` disables Unix-socket creation. The only client endpoint remains `127.0.0.1:56432` mapped to container TCP port `6432`.
- PgBouncer release archive SHA-256: `924ad35113fd0a71c8e2dbe85b5d03445532e2b7b37a9f8a48983beea238b332`
- Alpine 3.22.5 OCI manifest: `sha256:14358309a308569c32bdc37e2e0e9694be33a9d99e68afb0f5ff33cc1f695dce`
- Dockerfile SHA-256: `285d313814ca697d41e1c41ea69630864bf1706424542064c5daf3f1b0cafc57`
- compose.yaml SHA-256: `f097946ed059b7441adffb1a1be1745ad3c94b8f2e64465750536890099d1877`
- pgbouncer.ini/configuration SHA-256: `2b87ed7f8f9303144fb37b93d6a6165ffcf1fbc169991c48cf5b5d8a3720ff4c`
- Canonical infrastructure digest: `4eebd2a034d9e8797bc2c2e147a90dc688fb49a3eac39f9d1ce3d0a298c1e1d6`

The runner reads these three fixed repository paths itself, resolves each canonical path inside the fixed `pd6f-pooler` directory, computes each SHA-256, and parses the pinned product, version, transaction mode, network alias, and loopback binding. There is no environment hash, caller path, shell command, Docker command, PgBouncer admin SQL, or backend identity input.

The Dockerfile eliminates mutable package-index resolution. Its complete x86_64 build closure is fetched from exact versioned Alpine v3.22 artifact URLs, and every APK has an inline BuildKit SHA-256. `apk add --no-network` installs only those locally verified files. The PgBouncer release is likewise an exact URL with an inline SHA-256. Future builds require internet to retrieve artifacts, but content selection is immutable and a changed artifact is rejected. The builder invokes the explicit PgBouncer 1.25.2 `pgbouncer` binary target and installs that binary directly; documentation/manpage targets and `pandoc` are outside build authority. The runtime copies only the binary and four verified runtime APKs from the builder; compiler, documentation, source, headers, and build tools remain absent.

The later infrastructure-setup phase must produce the closed external record `C:\AFEX-Evidence\A2.5-PD6F\pd6f-infrastructure-attestation.json`. The runner always derives this filename from the already validated output directory; there is no environment or CLI path override. The record is a gate, not canonical scenario evidence.

Its exact 15-key object has `additionalProperties=false`: `schema_version`, `pooler_product`, `pooler_version`, `pooler_mode`, `image_id`, `dockerfile_sha256`, `compose_sha256`, `pgbouncer_config_sha256`, `infrastructure_digest`, `host_binding`, `container_port`, `backend_alias`, `backend_port`, `network_name`, and `attestation_digest`. Constants are respectively `A2.5-PD6F-INFRA-1`, `PGBOUNCER`, `1.25.2`, `TRANSACTION`, an inspected image ID matching `^sha256:[a-f0-9]{64}$`, the three hashes above, the infrastructure digest above, `127.0.0.1:56432`, integer `6432`, `pd6f-postgres-backend`, integer `5432`, and `afex-pd6f-isolated`. SHA fields are lowercase 64-character hexadecimal strings.

`attestation_digest` is SHA-256 over the runner's canonical serialization of the other 14 fields: object keys sorted lexicographically at every level, arrays retained in order, JSON primitive encoding, and no whitespace. It excludes itself and excludes timestamps, container IDs, usernames, secret paths, passwords, userlist content, PIDs, and logs. Any authority-field change changes the recomputed digest and is rejected.

After setup, the human operator runs only these bounded, read-only inspections from the worktree (shown here for future authorization; **do not run in this phase**):

```powershell
docker image inspect afex-pd6f-pgbouncer:1.25.2 --format '{{.Id}}'
docker inspect --type container afex-pd6f-pgbouncer --format '{{.Image}}'
docker inspect --type container afex-pd6f-pgbouncer --format '{{json .HostConfig.PortBindings}}'
docker inspect --type container afex-pd6f-pgbouncer --format '{{json .NetworkSettings.Networks}}'
docker inspect --type container afex-a24e-pg17 --format '{{json (index .NetworkSettings.Networks "afex-pd6f-isolated").Aliases}}'
docker image inspect afex-pd6f-pgbouncer:1.25.2 --format '{{json .Config.Labels}}'
docker inspect --type container afex-pd6f-pgbouncer --format '{{json .Config.Labels}}'
Get-FileHash -Algorithm SHA256 -LiteralPath runtime-integration\pd6f-pooler\Dockerfile
Get-FileHash -Algorithm SHA256 -LiteralPath runtime-integration\pd6f-pooler\compose.yaml
Get-FileHash -Algorithm SHA256 -LiteralPath runtime-integration\pd6f-pooler\pgbouncer.ini
```

The operator copies only the bounded authority values into the exact object and computes its canonical digest. An independent reviewer must accept the generated record before runner execution. The runner recomputes repository hashes and both digests and rejects any mismatch before creating a pool, but it deliberately contains no shell/Docker execution and therefore cannot cryptographically self-certify live Docker state. Operator inspection plus independent review supplies that boundary without overclaiming.

## Dedicated backend network lifecycle

Infrastructure setup, not this phase, creates external bridge network `afex-pd6f-isolated`, attaches existing container `afex-a24e-pg17` with alias `pd6f-postgres-backend`, and starts the Compose service on that network. PgBouncer then reaches `pd6f-postgres-backend:5432` without changing PostgreSQL's existing `127.0.0.1:55432` host publication. No application or production network is attached. Authorized cleanup stops PgBouncer, disconnects the PostgreSQL container, and removes the temporary network; no PostgreSQL restart is required.

## Secret and output boundaries

The future operator creates `C:\AFEX-Secrets\A2.5-PD6F\userlist.txt` from a secure prompt, restricts its ACL, mounts it read-only using `AFEX_A25_PD6F_USERLIST_PATH`, and keeps it outside Git. Its exact single-line format is `"afex_a25_pd6_runner" "PLAINTEXT_PASSWORD_VALUE"`, where the second quoted value is a documentation placeholder and never a repository literal credential. PgBouncer 1.25.2 uses that plaintext secret to perform SCRAM authentication on both the client-to-PgBouncer and PgBouncer-to-PostgreSQL legs; no separately salted verifier is generated or assumed identical to PostgreSQL's stored verifier. It is temporary and may be deleted only after evidence review and cleanup authorization. The runner receives the same password only through `AFEX_A25_PD6_PASSWORD`. Neither secret nor the external file is logged, serialized, hashed, or digested. No `auth_query` or role mutation is used.

`AFEX_A25_PD6F_OUTPUT_DIR` must resolve to an absolute external directory. The reviewed future target is `C:\AFEX-Evidence\A2.5-PD6F`. Repository/worktree/dirty-source descendants and reparse aliases into them are rejected. Existing finals are never overwritten. All temporary files are prepared first, then results and digest are renamed, with the summary published last. Any failure rolls back current-attempt files and remains nonzero.

`AFEX_A25_PD6F_DIRTY_SOURCE` is a mandatory operator-local absolute path identifying the separate dirty source tree that must also be excluded from evidence publication. It is not a secret, canonical input, authority hash, evidence field, digest field, or published value. Missing, empty, whitespace-only, or relative values fail as `OUTPUT_PATH_INVALID` before infrastructure verification or pool creation. This environment-derived input replaces the developer-specific path that existed during the accepted historical run. The portability correction was applied after that run, does not claim real PostgreSQL execution by the corrected source, and does not authorize another PD6F execution.

Final files are `pd6f-results.json`, `pd6f-digest.txt`, and `pd6f-summary.txt`.

## Scenario and compatibility contract

The aggregate registry contains only `PD6-D-032`. Its ordered checks are `POOLER_BASELINE`, `UNNAMED_QUERY`, `TRANSACTION_COMMIT`, `ORDINARY_FAILURE_ROLLBACK`, `STATEMENT_TIMEOUT`, `RESULT_BEFORE_DEADLINE`, `DESTRUCTIVE_REPLACEMENT`, `CHECKOUT_CONTENTION`, `SANITATION_REACQUISITION`, `SHUTDOWN_BEFORE_BORROWER`, `NO_SESSION_AFFINITY`, and `NO_NAMED_STATEMENTS`.

All queries use the unchanged 18-operation trusted fixture vocabulary and unnamed query descriptors. The unchanged `RealDriverAdapter` controls transaction, rollback, sanitation, ownership, replacement, and release. There is no retry. PASS never depends on backend PID, socket identity, temp objects, named statements, prepared statements, session GUCs, advisory locks, or LISTEN/NOTIFY. Transaction-local `SET LOCAL` is allowed.

The successful bounded result is `COMPLETED_SAFE`, `NORMAL_RELEASE`, no failure or SQLSTATE, IDLE before/after, zero retained rollback attempts, and `retry_attempted=false`. Aggregate/digest/publication occur only after successful bounded pool shutdown.

The commit check first verifies the seeded transaction row, commits the reviewed mutation, observes it, then uses the existing trusted update operation in a second transaction to restore revision `0` and payload `pd6-transaction-baseline`. A final trusted read must prove the exact seed before PASS. Any failure after the mutation commit attempts restoration; restoration failure remains nonzero and prevents aggregate construction or publication.

## Accepted real transaction-pool run attestation

The independent evidence review approved the real run as `A2_5_PD6F_REAL_POOLER_EVIDENCE_REVIEW_APPROVED` with final marker `A25PD6FEVIDENCEREVIEW_900_REAL_TRANSACTION_POOL_COMPATIBILITY_EVIDENCE_ACCEPTED`.

The accepted external evidence identities are:

| File | Size (bytes) | SHA-256 |
|---|---:|---|
| `pd6f-infrastructure-attestation.json` | 862 | `748c6ee88152f92bc1d27c9e710dbdda3f170516eda8c4b11085e58b0d677915` |
| `pd6f-results.json` | 2229 | `7026596b08b3cd4baba403abeeed12231ad93ad85bb3b4348119fb9800d49812` |
| `pd6f-digest.txt` | 65 | `0c5427cfd48006752be0c363a33159c09be44f743c375e0c9156f1e01be00ef4` |
| `pd6f-summary.txt` | 131 | `8a0659305b97ac62cdd6ef60988186082873bc8f93e0c528f91aab0b4f908ee0` |

- Accepted canonical evidence digest: `ecdaa6f3262a413345860787ea3cd7f137f74186919d3aa4ef98aa9073c5d7ef`
- Independently recomputed infrastructure-attestation digest: `19abdefc5cb42dd8d8180b6b6b5b5f1ba8262c79041b207805add0c2e2533750`
- Runtime authority: schema `A2.5-PD6F-POOLER-1`; driver `8.22.0`; PostgreSQL `17.10`; endpoint `PGBOUNCER_TRANSACTION_POOL`; tier `PD6F_POOLER`; product/version/mode `PGBOUNCER` / `1.25.2` / `TRANSACTION`.
- Scenario authority: exactly one scenario, ordered as `PD6-D-032`; result count `1`; order count `1`; `aggregate_pass=true`; `failed_scenarios=NONE`.
- Execution accounting: the missing-`pg` attempt failed during module import with zero protocol executions; the credential-required attempt failed before connection with zero protocol executions; exactly one accepted real protocol run executed and passed; the evidence review performed zero additional runs.
- Regression authority: PD6F `102/102`, PD6 `57/57`, and PD5 `66/66` passed.
- Evidence-review boundary: database connections `0`, SQL executions `0`, and runner executions `0` during review.
- Review integrity: all four evidence files remained byte-for-byte unchanged; secret/raw-error leakage findings were `0`; the repository remained unchanged.
- Infrastructure at acceptance: PgBouncer and PostgreSQL were running, restart count remained `0`, and infrastructure cleanup had not been performed.

This amendment records an already accepted run and does not authorize another execution. Staging, commit, push, pull request creation, merge, infrastructure cleanup or retirement, Provider testing, Supavisor testing, and Production work remain unauthorized until their explicit later gates.

## Exact exit map

| Exit | Diagnostic |
|---:|---|
| 0 | complete success only |
| 10 | `CREDENTIAL_REQUIRED` |
| 11 | `OUTPUT_PATH_INVALID` |
| 12 | `INFRASTRUCTURE_ATTESTATION_INVALID` |
| 13 | `POOLER_CONNECT_FAILED` |
| 14 | `POOLER_BASELINE_FAILED` |
| 15 | `POOLER_TRANSACTION_FAILED` |
| 16 | `POOLER_SANITATION_FAILED` |
| 17 | `POOLER_CHECKOUT_TIMEOUT_MISMATCH` |
| 18 | `POOLER_SHUTDOWN_FAILED` |
| 19 | `SCHEMA_VALIDATION_FAILED` |
| 20 | `DIGEST_FAILED` |
| 21 | `OUTPUT_WRITE_FAILED` |
| 22 | `UNEXPECTED_RUNNER_FAILURE` |

Raw driver, PostgreSQL, PgBouncer, filesystem, and stack errors are never emitted.

## Historical human procedure — no further execution authorized

The completed human procedure created the dedicated network, attached `afex-a24e-pg17` with the frozen alias, created the restricted external plaintext-secret userlist, verified all repository hashes, built the pinned image, and started only `pd6f-pgbouncer`. The closed Docker inspection and infrastructure attestation were independently reviewed before the single accepted runner invocation. This historical description does not authorize repetition. Cleanup remains separately gated. No provider endpoint was involved.
