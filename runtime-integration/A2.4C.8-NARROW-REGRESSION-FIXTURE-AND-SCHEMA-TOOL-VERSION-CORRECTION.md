# A2.4C.8 Narrow Regression-Fixture and Schema/Tool Version-Validation Correction

Decision: `NARROW_REGRESSION_CORRECTIONS_COMPLETE`

## Corrections

- Expanded the authoritative-projection fixture with fixed synthetic `token`, `password`, `connectionString`, `rawHost`, `stack`, `arbitraryPayload`, and `queryText` fields. Every prohibited key and marker value must be absent, while the approved neighboring `sourceStatus` remains present.
- Added deterministic runner fixtures using the same retained-results validator and classification function as the operational path. They cover nonzero valid `INCOMPLETE`, digest mismatch, malformed JSON, wrong run ID, wrong schema, missing mandatory field, timeout with valid retained evidence, and timeout with invalid evidence.
- Routed fixed evidence-schema and harness-tool versions through the shared bounded version validator.
- Preserved integrity-validation-before-classification, one authoritative evidence projection, fail-closed timeout handling, bounded termination, exact result bindings, and the hard-disabled execution gate.

## Boundaries

A2.4C.7 approved the major result-integrity, unified-redaction, traceability, and status controls. This phase adds only the missing regression coverage and removes the fixed-version validator bypass. Runtime evidence, provider authority, operator inputs, dependency installation, and execution authorization remain absent. Core V2 remains hard-disabled.

No SQL, runner, harness, child process, package installation, database/provider contact, credential addition, Git write, Production access, P2D.20 call, or Core V2 activation occurred.

## Subsequent compatibility correction

A2.4C.9 independently found that the valid runner fixtures depended on a two-space JSON layout not produced by Windows PowerShell 5.1 `ConvertTo-Json`. A2.4C.10 is the authoritative correction for that formatting dependency and preserves this document as the historical attempted fixture correction.

A24C8_900_NARROW_REGRESSION_FIXTURE_AND_SCHEMA_TOOL_VERSION_CORRECTIONS_COMPLETE
