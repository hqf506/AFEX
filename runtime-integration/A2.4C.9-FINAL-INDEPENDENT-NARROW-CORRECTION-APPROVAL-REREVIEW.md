# A2.4C.9 Final Independent Narrow-Correction Approval Rereview

Decision: `NARROW_SOURCE_CORRECTIONS_REQUIRED`

## Executive verdict

The expanded negative-redaction fixtures and shared schema/tool version validation are source-correct. The runner outcome fixtures are not approved because their supposedly valid JSON is produced by Windows PowerShell 5.1 `ConvertTo-Json`, which indents top-level properties with four spaces, while the operational contract validator requires exactly two. The first valid-INCOMPLETE fixture therefore fails as evidence-invalid before reaching its expected nonzero-exit classification, and the source fixture gate would stop every future authorized runner invocation before child-process launch. This is one narrow compatibility correction, not an architectural redesign.

## Repository preconditions

- Branch `master`; HEAD and `origin/master` are `4fd239784ea6d38e1744b2b62d664aa33b02496c`.
- Ahead/behind `0/0`; staged files `0`; no merge, rebase, cherry-pick, or revert is active.
- Visible paths are confined to A2.4B.3 through A2.4C.8; no unrelated path was found.
- `git diff --check` passes.

## Review coverage

The runner, harness, evidence contract, manifest, blocker status, A2.4C.6 through A2.4C.8 records, and traceability index were read completely and cross-checked against all three A2.4C.7 findings.

## Negative-redaction fixture findings

Decision: `NEGATIVE_REDACTION_FIXTURES_APPROVED`.

Harness lines 208-215 explicitly cover `token`, `password`, `connectionString`, `rawHost`, `stack`, `arbitraryPayload`, and `queryText` with fixed synthetic markers. The fixture serializes only `projectCaseResult(fixture)`, requires the safe neighboring `SAFE_FIXTURE_PRESENT` value, and rejects both every prohibited key and its marker value. The projection is deterministic, bounded, non-recursive, and contains no real secret, URL, hostname, token, or business identifier. No duplicate/manual final projection path was introduced.

## Runner outcome fixture findings

Decision: `RUNNER_OUTCOME_FIXTURES_CORRECTION_REQUIRED`.

The fixtures list all eight required outcomes and route them through `Invoke-RunnerOutcomeFixture`, which calls `Test-RetainedResultsContract` before `Resolve-RunnerClassification`. They launch no child process and perform no network, database, or package operation. However, runner line 160 creates valid fixture JSON using Windows PowerShell 5.1 `ConvertTo-Json`. Independent local compatibility evidence shows that this runtime emits four leading spaces before top-level properties, whereas validator line 98 requires exactly two through `(?m)^  "field":`. Consequently, lines 164 and 181 cannot demonstrate valid retained evidence: validation throws `Required top-level JSON field schemaVersion is missing or duplicated` before the expected native-exit or timeout classification. The wrong-schema and missing-field fixtures are similarly masked by indentation failure.

## Shared version-validation findings

Decision: `SHARED_VERSION_VALIDATION_APPROVED`.

The shared validator enforces required/optional presence, 64-character maximum, trim equality, CR/LF rejection, approved version grammar, lowercase normalization, and `NOT_INSTALLED` for absent optional packages. Node, npm, `pg`, optional `@types/pg`, optional `@vercel/functions`, evidence-schema, and harness-tool versions all pass through it. No runner-contract or manifest schema/version value is serialized. No fixed schema/tool bypass or raw unvalidated version environment serialization remains.

## Source-level invariant findings

- All seven required sensitive fields are covered and omitted while a safe neighboring value remains present.
- The runner fixture wrapper orders integrity validation before classification, but its valid fixture input is incompatible with the exact operational JSON layout check on Windows PowerShell 5.1.
- Every serialized version originates from the shared validator.
- Only the projected negative fixture and authoritative final evidence object are passed to `JSON.stringify`.
- No direct result/error/environment/configuration serialization bypass or real secret-like fixture was found.

## Regression review

No regression was found in operational nonzero-exit validation ordering, independent digest recomputation, JSON bindings, unified redaction, bounded process-tree termination, assertion IDs, denial expected/actual SQLSTATE, mandatory final-status binding, timeout hierarchy, partial-setup runbook, traceability, environment restoration, or hard-disabled gates. The new fixture compatibility defect prevents runner progression but does not weaken the operational fail-closed contract.

## Manifest findings

- Rows: `12`.
- Relative-path, line-count, byte-count, SHA-256, purpose, execution-status, and review-status mismatches: `0`.
- Missing/stale rows: `0`.
- Self-hash treatment is explicit and non-recursive.
- Independently recomputed manifest SHA-256: `a5d305da3e5a34aa4e9576f0694c4c7d7609a2189854aeaa4f0751ddc74e8a83`.

## Status consistency

The current documents accurately state that A2.4C.7 approved the major controls and found bounded fixture/version gaps, and that A2.4C.8 attempted only those corrections. Runtime evidence, provider authority, operator inputs, approved installed dependencies, and execution authorization remain absent. Core V2 remains hard-disabled. This review supersedes the A2.4C.8 completeness claim only for the PowerShell fixture-format defect.

## Independent static validation

- Windows PowerShell 5.1 parser errors: `0`.
- Windows PowerShell version: `5.1.26100.8972`; direct `ConvertTo-Json` compatibility probe showed four-space top-level indentation.
- PowerShell 7: unavailable locally; no result claimed.
- Node TypeScript syntax: PASS.
- `npx tsc --noEmit`: PASS.
- Negative-redaction scan: PASS.
- Runner-outcome inventory: complete; valid-input compatibility: FAIL.
- Shared-version-validator scan: PASS.
- Direct serialization bypass scan: PASS.
- Credential/full-URL scan: no finding.
- Manifest rows/mismatches: `12/0`.
- UTF-8 without BOM, LF endings, and trailing-whitespace scan: PASS.
- `git diff --check`: PASS; staged files `0`.

## Findings matrix

| Severity | Exact path and line | Issue | Failure scenario | Violated contract | Required correction | Execution impact | Commit impact |
|---|---|---|---|---|---|---|---|
| HIGH | `database-reconciliation/core-v2/P2D/A2.4B.3-RUN-ISOLATED-DIRECT-EXECUTE-TESTS.ps1:98,151-182` | PowerShell 5.1 fixture JSON uses four-space indentation but the retained-results validator accepts only the harness's two-space top-level layout. | The first valid-INCOMPLETE source fixture fails at `schemaVersion` discovery, so `Test-RunnerOutcomeSourceFixtures` stops the runner at `source_contract_fixtures` before process launch; valid nonzero and timeout transitions are never tested. | Deterministic source fixtures must use valid operational-contract evidence and assert the intended state transition across Windows PowerShell 5.1 and PowerShell 7. | Construct fixture JSON with the exact deterministic two-space/LF harness representation, or introduce a narrowly scoped deterministic fixture serializer; preserve the operational duplicate-field and canonical-layout checks. Then statically prove all expected messages remain reachable. | Blocks source approval and every future authorized harness launch through this runner. No database or SQL effect occurred. | Runner, runner manifest row, evidence contract/status, and a narrow correction record. |
| INFORMATIONAL | `database-reconciliation/core-v2/P2D/A2.4B.3-ISOLATED-DIRECT-EXECUTE-HARNESS.ts:208-215,259-266` | Negative-redaction and shared-version corrections are complete. | None. | A2.4C.7 bounded findings. | None. | No execution authority granted. | No correction required. |

## Readiness decision

`NARROW_SOURCE_CORRECTIONS_REQUIRED`

The HIGH fixture-format incompatibility prevents approval for external gate collection. This review grants no SQL execution, dependency installation, database/provider contact, Production testing, Git write, or Core V2 activation authority.

## Boundaries and next phase

No existing file was modified. No SQL, runner, harness, or package was executed; no database/provider was contacted; no credential was added; no staging, commit, or push occurred. The small `ConvertTo-Json` compatibility probe serialized a fixed in-memory sample only and performed no child-process, file, network, database, or provider operation.

Provider authority, operator inputs, dependency approval, disposable runtime evidence, cleanup/absence proof, and execution authorization remain external blockers rather than source defects.

Exact next phase: `A2.4C.10 — PowerShell 5.1 Deterministic Runner-Fixture JSON Compatibility Correction`, followed by one final independent source rereview.

A24C9_900_FINAL_INDEPENDENT_NARROW_CORRECTION_APPROVAL_REREVIEW_COMPLETE
