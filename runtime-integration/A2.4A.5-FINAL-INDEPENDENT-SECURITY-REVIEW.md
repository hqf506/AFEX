# A2.4A.5 Final Independent Security and Boundary Review

Status: CORRECTION_REQUIRED

## Executive verdict

The hard-disabled adapter, frozen transport contract, fake isolation, package/environment/P2D.20/role-target controls, and repository no-activation state remain intact. Independent adversarial testing found one unresolved HIGH generic-capability provenance bypass. A commit is not approved.

## Repository preconditions

- Branch: `master`.
- HEAD and `origin/master`: `e6260732cbdd5a3478fcb5f1130518c58ad7225d`.
- Staged files: zero.
- Active merge, rebase, cherry-pick, or revert: none.
- Visible changes are confined to A2.4A through A2.4A.4 scope.
- Initial `git diff --check`: PASS.

## Review coverage

Every A2.4A adapter, boundary-rule, scanner, fixture/type-test, and A2.4A through A2.4A.4 document was reviewed. Existing fixtures were rerun, and additional in-memory adversarial scanner inputs were evaluated without modifying repository files.

## Transport and disabled-factory findings

The transport exposes exactly `acquire`, aliases the frozen eight-field input and raw result, and exposes no query, transaction, connection, callback, role-target, or function-name parameter. The returned disabled transport is frozen, has an own `acquire` implementation that always throws the stable `CORE_V2_ADAPTER_DISABLED` error, reads no environment, and performs no network/database work. No finding.

## Generic-capability finding

### HIGH — function-expression and object-method return provenance is lost

- Path and lines: `scripts/check-core-v2-boundaries.mjs:430-449`.
- Issue: `functionCapabilities` is populated only for `FunctionDeclaration`. `provenanceOf` resolves calls only when the called identifier is already present in that map. Function expressions assigned to variables and object methods are not analyzed as capability-returning wrappers.
- Reproduced bypasses, each producing zero `adapter_generic_query_surface` findings:

```ts
const getRunner = function () { return client.query }
export const run = getRunner()
```

```ts
const wrapper = { getRunner() { return client.query } }
export const run = wrapper.getRunner()
```

- Failure scenario: an adapter module can export a generic database capability through a statically resolvable local wrapper while the scanner exits successfully.
- Violated contract: A2.4A.4 requires provenance through function return values and local wrapper functions where statically resolvable.
- Required correction: extend provenance collection to named variable function expressions/arrow functions and statically resolvable object methods; resolve their call sites and retain origin, wrapper, alias/export, and final-use coordinates. Add isolated failing fixtures for each form and safe controls.
- Commit impact: blocks commit approval.

An expression-bodied arrow containing `client.query` currently produces a finding only at the direct member read; it does not prove the complete wrapper-to-export provenance required by the diagnostic contract. The correction should make that chain explicit rather than relying on incidental direct-member detection.

## Package, environment, P2D.20, role-target, and fake findings

The requested existing fixtures cover package families/subpaths, import forms, wrappers/barrels/cycles, environment forms, P2D.20 invocation forms, role-target variants, and fake import denial. The live repository scan passes. The fake snapshots raw rows, nested structures, bytes, dates, failure metadata, returned rows, and disposition evidence without retaining caller-owned mutable references. Only the two approved test scripts may reach the fake. No additional finding.

## Scanner implementation findings

Ordering and final finding deduplication are deterministic; graph closure terminates on cycles; paths are normalized; sources are parsed but not executed. The unresolved wrapper gap above also means the A2.4A.4 documentation claim that local-wrapper provenance is complete is inaccurate until corrected.

## Static limitations and frozen contracts

The documents correctly retain limitations for nonliteral dynamic imports, arbitrary runtime-computed names, monkey-patching, generated/evaluated code, symbol-only members, and information-erasing casts. The P2D.20 mapping, one-operation transport, authorization provenance, idempotency, replay, safe errors, outbox/lifecycle semantics, disabled posture, no service-role fallback, no dual write, and no activation remain unchanged.

## Validation results

- TypeScript: PASS.
- ESLint: PASS with zero errors and six unrelated pre-existing warnings.
- Contract tests: PASS, 216.
- Scanner fixtures: PASS, 79.
- Stable rules: 19; isolated fixture coverage: 19; uncovered rules: zero.
- Adapter type tests: PASS through TypeScript and contract validation.
- Live boundary scan: PASS.
- Production build: PASS, 114 pages.
- `git diff --check`: PASS before this report; final verification required after report creation.
- Independent wrapper-expression adversarial tests: FAIL as documented above.

## Findings matrix

| Severity | Finding | Status | Commit impact |
| --- | --- | --- | --- |
| HIGH | Function-expression/object-method capability-return provenance bypass | Unresolved | Blocks commit |
| MEDIUM | None | — | — |
| LOW | None | — | — |
| INFORMATIONAL | Six unrelated existing lint warnings | Existing | Does not affect this gate |

## Commit readiness and next phase

Decision: `CORRECTION_REQUIRED`.

The exact next phase is a narrow A2.4A.6 correction for capability-return provenance in function expressions, arrow functions, and object methods, with isolated adversarial fixtures and a fresh independent rereview. This review does not authorize package installation, concrete transport work, SQL/database/provider activity, route integration, staging, commit, push, or activation.

A24A5_900_FINAL_INDEPENDENT_SECURITY_REVIEW_COMPLETE
