# A2.4C Minimum Source Correction Freeze

Status: FROZEN SCOPE; A2.4C.1 OVERSTATEMENT CORRECTED BY TARGETED A2.4C.3 WORK; PENDING INDEPENDENT REREVIEW

Only the following source corrections block execution-readiness. Missing provider answers, operator values, dependency approval, and runtime proof are deliberately excluded.

| Path | Exact issue | Required change | Unsafe without correction | Required validation |
|---|---|---|---|---|
| `database-reconciliation/core-v2/P2D/A2.4B.3-DIRECT-EXECUTE-TEST-ATTESTATION.sql` | Direct/effective privilege proof is incomplete; relation scanning omits effective `SELECT`; exact overload, column ACL, default ACL, unrelated-function, schema, sequence, ownership, and transitive membership inventories are absent. | Implement one read-only, fail-closed expected-set attestation covering LOGIN attributes; direct/transitive memberships; ownership; exact probe signature, owner, `SECURITY DEFINER`, fixed `search_path`; direct/effective EXECUTE; PUBLIC/managed-role denials; table, column, sequence, schema, unrelated-function denials; absence from `afex_core_runtime`; unambiguous marker and rollback. | A privilege leak or unexpected grant could be accepted. | PostgreSQL 17.6 static syntax review; mutation scan; exact expected-set review; one read-only BEGIN, one ROLLBACK, marker reachable only after all assertions. |
| `database-reconciliation/core-v2/P2D/A2.4B.3-RUN-ISOLATED-DIRECT-EXECUTE-TESTS.ps1` | No repository proof, evidence containment/ignore proof, pinned hash comparison, timeout/child termination, completion-marker/result validation, or all-path summary; runner gate differs from harness gate. | Discover and prove the repository root; constrain a normalized evidence root beneath the ignored P2D evidence directory; compare every executable artifact to a frozen manifest; use the exact authorization literal; launch with a bounded timeout; terminate the process tree on timeout; retain stdout/stderr; always write a safe summary with launch/timeout/exit/marker status; restore environment in `finally`; fail on nonzero exit or missing/invalid final evidence. | Arbitrary or changed source, unsafe paths, a hung child, or incomplete output could be treated inconsistently. | Windows PowerShell 5.1 and PowerShell 7 parser checks; simulated launch failure, timeout, nonzero exit, missing marker, and success; secret/full-URL scan; containment tests; manifest mismatch test. |
| `database-reconciliation/core-v2/P2D/A2.4B.3-ISOLATED-DIRECT-EXECUTE-HARNESS.ts` | Post-ROLLBACK identity is absent; sequential/concurrent cases only collect observations; unavailable controllers are lists rather than implementations; complete evidence metadata and final marker are absent. | Add post-ROLLBACK and reacquisition identity assertions; assert expected session/current identity, PID boundaries, fixed probe result, denial SQLSTATEs, contamination invariants, and concurrent correlations; retain fixed unnamed statements, bounded pool, and quarantine; emit the frozen evidence schema and a completion marker only when every available assertion passes; remain `INCOMPLETE` while any mandatory controller is unavailable. | The harness can collect data without proving the required invariant, and the runner cannot bind completion to evidence. | TypeScript syntax and `npx tsc --noEmit`; fixed-operation scan; no generic/caller SQL; no P2D.20/business data; deterministic fixture tests for every assertion and redaction path. |
| `runtime-integration/A2.4B.3-PASS-FAIL-AND-EVIDENCE-CONTRACT.md` | Required evidence is described but not mapped to exact machine fields and marker semantics. | Freeze the JSON/summary schema, required fields, allowed states, hash binding, redaction rules, and exact completion-marker conditions. | Runner and reviewers could interpret incomplete evidence differently. | Schema-to-harness-to-runner field mapping with no optional activation evidence. |
| `runtime-integration/A2.4B.3-OPERATOR-RUNBOOK.md` | Procedure references behavior the current runner/harness do not implement and lacks exact STOP ordering for partial source/runtime states. | Synchronize the procedure to the corrected hashes, gates, phases, evidence, timeout, quarantine, cleanup, and STOP rules; retain separate execution authorization. | An operator could proceed past an incomplete or ambiguous step. | Line-by-line mapping to runner states and evidence contract. |
| `runtime-integration/A2.4B.3-HASH-MANIFEST.md` | Counts and hashes are stale after A2.4B.6 changes. | Regenerate only after all correction artifacts are frozen; pin every execution-critical artifact and record review status. | Runner integrity checks cannot rely on the manifest. | Recompute line, byte, and SHA-256 values independently; zero mismatch. |
| `runtime-integration/A2.4B.3-OPEN-BLOCKERS-AND-NEXT-DECISION.md`, `runtime-integration/A2.4B.3-HARNESS-DESIGN.md`, and A2.4B.5E/6 status documents | Status and capability descriptions no longer form one synchronized execution gate. | Update only factual source status, implemented-controller status, and next gate after corrections; preserve all provider/operator blockers. | Documentation could overstate readiness or contradict executable behavior. | Cross-document status and controller matrix comparison. |

No setup/cleanup mutation redesign is authorized by this freeze. Provider-dependent role, ownership, disablement, revocation, session termination, and cleanup source must remain hard-blocked until the provider evidence gate is resolved.
