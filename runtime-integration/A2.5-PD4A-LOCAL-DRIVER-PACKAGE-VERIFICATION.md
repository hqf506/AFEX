# A2.5.PD.4A Local Driver Package Verification

## Exact file hashes

| File | SHA-256 |
|---|---|
| `runtime-integration/package.json` | `65d4d25f884d6bbd6202d0aaf1bfff43f165d2ca1b80e4257746fc3fe65fa525` |
| `runtime-integration/package-lock.json` | `dfb56ec8daaebfb1e4423568943dc023714e2c3b410dcc42137fa4c942d8954e` |
| `runtime-integration/A2.5-PD4A-LOCAL-DRIVER-PACKAGE-INSTALLATION-RECORD.md` | `cf81ecb12a9b3576c79baddae2bf1f567510ab5d440fddf140e7cf1e7a8601b5` |

The verification file's own final hash is reported by the enclosing review evidence because a file cannot embed its own cryptographic hash without changing it.

## Package manifest audit

The manifest name is `afex-runtime-integration`, version is `0.0.0`, `private` is true, and module type is `module`. Its only direct runtime dependency is exact `pg@8.22.0`; its only direct development dependency is exact `@types/pg@8.20.4`. It contains no ranges, scripts, workspaces, engines, author, license, repository, or publishing configuration.

## Lockfile audit

- Lockfile version: `3`
- Root name/version: `afex-runtime-integration@0.0.0`
- Resolved nodes: `17`
- Production/development/optional nodes: `14` / `3` / `1`
- Optional installed package: `pg-cloudflare@1.4.0`
- Forbidden Next, React, Vercel, and `pg-native` packages: none
- Drift from the approved two-run preflight graph: none

## Installed versions and integrity

- Installed `pg`: `8.22.0`
- Installed `@types/pg`: `8.20.4`
- `pg` integrity: `sha512-8wih1vVIBMxoUM2oB4soJsD9tDnDpLv4OXBJ+EJzFsvycD+lfyIreC2gGHq78f8jbLLt+bvlPTFdFZfJkOuzAA==`
- `@types/pg` integrity: `sha512-Jz7UDOlIiFJuacC0TlBoLyNtmwlA/wpIyPDd3tvUqlRM+HzkWy2xUgpFpaXtbfTAFF6sIGq5lsCDBdJnhky1Xg==`

## Module-loading verification

Named ESM imports returned `Pool` and `Client` constructors and the `types` object. CommonJS `require("pg")` exposed the same surfaces but remains unselected for future implementation. A Pool constructed with explicit inert values reported zero total, idle, and waiting clients; ending it produced zero intercepted socket connection attempts. No database environment variable was available to the check.

## Type declaration verification

The installed declaration package contains three declaration files. Static symbol inspection found declarations or references for `Pool`, `PoolConfig`, `PoolClient`, `Client`, `QueryConfig`, `QueryResult`, and `QueryResultRow`.

No TypeScript compiler is installed in this clean worktree. A no-emit compilation could not run without installing an unapproved compiler or changing module-resolution configuration. The declaration files and required symbols are present, so this is recorded as the explicitly permitted non-blocking tooling limitation.

## Root-package isolation

The root manifest and lockfile preserve their approved hashes and contain no `pg` node. No root `node_modules/pg` exists. All installed modules are confined to ignored `runtime-integration/node_modules`.

Approved preserved hashes:

- Root `package.json`: `e51294b9e857dbab5875bb495ba81b9d7973bb84fee0ebe3cead2a5854be5e9c`
- Root `package-lock.json`: `e164cfc87ff322c8368c8ea07afbd9d10335a322629654c3794a4e52ba564933`
- `next.config.ts`: `d0950baa880be67bbdc68fe165a5cd665cafb97e9c938d9970d843510f415c15`
- `tsconfig.json`: `fbff01604d6cb54694be707f26e7a0eb21a6ef796b888c6a1dab1839fa7c6ccc`

## Security and lifecycle audit

- Audit vulnerabilities: `0`
- Peer conflicts: `0`
- Install/postinstall scripts: `0`
- Package binaries: `0`
- Native compilation: `0`
- Credentials, environment files, endpoints, hooks, or connection strings introduced: `0`

## Encoding and Git status

The four authorized files are UTF-8 without BOM, LF-only, end with a newline, and contain no trailing whitespace or unfinished-work markers. They are the only retained file changes. Nothing is staged. Generated modules are confined to `runtime-integration/node_modules` and remain unstaged.

## No-database and no-SQL confirmation

No PostgreSQL or provider connection was opened, no query was executed, and no executable SQL was created. Module verification was limited to imports, Pool construction, counters, and Pool termination.

## Next-phase authorization boundary

The next phase may review only the four authorized files and prepare a scoped commit. It must not create a real-driver adapter, SQL, database evidence, cancellation amendment, provider connection, or application integration.

## Formal result marker

`A25PD4A_900_LOCAL_DRIVER_DEPENDENCIES_INSTALLED_AND_VERIFIED`
