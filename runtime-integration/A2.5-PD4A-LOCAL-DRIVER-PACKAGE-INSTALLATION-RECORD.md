# A2.5.PD.4A Local Driver Package Installation Record

## Phase identity

- Phase: `A2.5.PD.4A`
- Baseline commit: `0fe8d6517b5787b8bc54a43396ce1edc96dc2c29`
- Branch: `a2-5-pd-local-preflight`
- Boundary: `ISOLATED_RUNTIME_INTEGRATION_PACKAGE`

## Isolated package boundary

The package boundary is `runtime-integration`. The repository-root package manifest, lockfile, Next.js configuration, and TypeScript configuration are excluded and retain their approved hashes.

## Exact installed packages

| Classification | Package | Version | Registry integrity |
|---|---|---:|---|
| Runtime dependency | `pg` | `8.22.0` | `sha512-8wih1vVIBMxoUM2oB4soJsD9tDnDpLv4OXBJ+EJzFsvycD+lfyIreC2gGHq78f8jbLLt+bvlPTFdFZfJkOuzAA==` |
| Development dependency | `@types/pg` | `8.20.4` | `sha512-Jz7UDOlIiFJuacC0TlBoLyNtmwlA/wpIyPDd3tvUqlRM+HzkWy2xUgpFpaXtbfTAFF6sIGq5lsCDBdJnhky1Xg==` |

`@vercel/functions` and `pg-native` were not installed.

## Toolchain and command

- Node.js: `24.18.0`
- npm: `11.16.0`
- Command: `npm install --ignore-scripts --no-fund --no-audit`
- Current directory: the isolated `runtime-integration` package boundary
- Cache: a unique system-temporary directory outside all repository worktrees
- Global and user npm configuration changes: none

## Lockfile and graph

- Lockfile version: `3`
- Resolved package nodes: `17`
- Production nodes: `14`
- Development nodes: `3`
- Optional nodes: `1`
- Optional dependency: `pg-cloudflare@1.4.0`
- Lifecycle or install scripts: none
- Package binaries: none
- Native compilation: none
- Peer conflicts: none
- Audit vulnerabilities: none

## Root-package exclusion

The root manifest and lockfile do not contain `pg`. No package was hoisted into a root `node_modules`; installed modules exist only beneath the authorized generated `runtime-integration/node_modules` directory and remain unstaged.

## Module and declaration checks

Named ESM imports for `Pool`, `Client`, and `types` loaded successfully. CommonJS loading remains compatible but is not selected. A Pool was constructed and ended with zero clients and zero socket connection attempts.

The installed declaration package contains `Pool`, `PoolConfig`, `PoolClient`, `Client`, `QueryConfig`, `QueryResult`, and `QueryResultRow`. A compiler invocation was not available because the clean worktree has no installed root TypeScript compiler and adding one was outside this phase; this is a non-blocking tooling limitation.

## Database, network, and SQL boundary

Package registry access was limited to npm installation and audit metadata. No PostgreSQL, Supabase, Supavisor, Vercel, or application endpoint was contacted. No database environment fallback was used during module checks. No query or SQL statement was created or executed.

## Rollback procedure

Delete only `runtime-integration/package.json`, `runtime-integration/package-lock.json`, `runtime-integration/node_modules`, this record, and `runtime-integration/A2.5-PD4A-LOCAL-DRIVER-PACKAGE-VERIFICATION.md`. Then verify the four root hashes and require the clean worktree to return to clean. Do not run broad clean/reset operations and do not alter the dirty source worktree.

## Limitations

This phase proves dependency installation, package isolation, module loading, lazy Pool construction, and declaration presence only. It does not prove database connectivity, queries, cancellation, transaction behavior, pooler semantics, provider behavior, or Production readiness.

## Next phase

`A2.5.PD.4A.REVIEW — Review Isolated Driver Package Installation and Prepare Scoped Commit`

## Formal completion marker

`A25PD4A_900_LOCAL_DRIVER_DEPENDENCIES_INSTALLED_AND_VERIFIED`
