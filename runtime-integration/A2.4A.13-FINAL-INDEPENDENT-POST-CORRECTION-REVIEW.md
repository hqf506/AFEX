# A2.4A.13 — Final Independent Post-Correction Security and Boundary Review

## 1. Executive verdict

**CORRECTION_REQUIRED**

A2.4A.12 fixes its direct fixtures, but fresh residual probes found four HIGH statically resolvable class/constructor provenance gaps and one MEDIUM diagnostic gap. Approval is prohibited while any HIGH remains.

## 2. Repository preconditions

- Branch: `master` — PASS.
- HEAD and `origin/master`: `e6260732cbdd5a3478fcb5f1130518c58ad7225d` — PASS.
- Staged files: zero — PASS.
- Merge, rebase, cherry-pick, or revert state: none — PASS.
- Visible paths: A2.4A–A2.4A.12 only — PASS.
- Unrelated paths: none detected — PASS.
- Initial `git diff --check`: PASS.

## 3. Review coverage

Reviewed the complete adapter tree, boundary rule declarations, scanner, contract/fixture runner, adapter type tests, A2.4A–A2.4A.12 documentation, and relevant frozen A1/A2 contracts. Independently exercised prior bypass families, focused A2.4A.12 cases, additional class/container compositions, safe controls, and non-generic rules.

## 4. Prior-bypass retest

Existing fixtures and independent direct probes reject member aliases, function forms, object methods/getters/function properties, computed declarations, ordinary object/array spreads, mapped/concrete conditional types, indexed tuples, direct tuple rest, normalized expressions, direct conditional/nullish selection, direct class expressions, direct class factories, direct constructor returns, namespace-bound class expressions, correct static/instance inheritance, wrappers, barrels, CommonJS, and cycles with `adapter_generic_query_surface`.

The residual combinations in section 10 are not represented by the existing fixtures and bypass the scanner.

## 5. Tuple-rest findings

Direct, nested, readonly, callable, alias, conditional-layout, multiple-spread, and hole-containing tuple-rest probes are blocked. Rest from a returned tuple also causes a rule finding because the forbidden member is independently observed at its definition, but the returned container is not connected through spread/rest to the final exposure. This fails the diagnostic provenance requirement and is MEDIUM M-1.

## 6. Conditional/nullish spread findings

Unsafe first indexes, shifted indexes, unresolved branches, nullish alternatives, nested selections, multiple spreads, and all-safe alternatives behave as intended for statically known container paths. Possible output offsets remain separated and unsafe indexes are preserved. No new direct alternative-layout bypass was found.

## 7. Class factory and constructor findings

Direct declaration/expression/arrow factories, inline classes, returned declared classes, direct multi-hop factory calls, namespace classes, and subclasses are covered. Two residual classes bypass:

- A factory returning an alias of a class loses identity because only original identities are held in `classIdentities`.
- A generic-looking identity wrapper with a concrete class argument is statically resolvable at the call site but has no parameter/argument class-surface propagation.

These are HIGH H-1 and H-2.

## 8. Namespace-bound class findings

Direct exported and nested namespace class expressions, aliases, static access, construction, and namespace alias chains are covered. A namespace-bound class returned directly by a factory is covered. No separate namespace-identity bypass was found beyond the object-property container issue in H-4.

## 9. Static/instance override findings

Same-name static/instance methods, computed names, safe overrides, unsafe inherited members, unsafe child overrides, multilevel inheritance, getters, fields, and detached safe references behave correctly in the tested direct class hierarchy. Separate declared-name sets prevent cross-namespace suppression. No new override-resolution bypass was found.

## 10. Residual adversarial classification

| Variant | Classification | Result |
|---|---|---|
| Tuple rest after multiple spreads | STATICALLY_COVERED | Rejected |
| Tuple rest from conditional alternatives | STATICALLY_COVERED | Rejected |
| Tuple rest from returned spread container | NEW_FINDING | Blocked only at source; final rest provenance absent |
| Nested rest pattern with holes | STATICALLY_COVERED | Rejected |
| Factory returning direct class | STATICALLY_COVERED | Rejected |
| Factory returning aliased class | NEW_FINDING | Bypassed |
| Factory returning subclass | STATICALLY_COVERED | Rejected |
| Factory returning namespace class | STATICALLY_COVERED | Rejected |
| Concrete generic-looking factory wrapper | NEW_FINDING | Bypassed |
| Constructor stored in tuple | NEW_FINDING | Bypassed |
| Conditional constructor selection | STATICALLY_COVERED | Rejected |
| Nullish constructor selection | STATICALLY_COVERED | Rejected |
| Computed same-name static/instance members | STATICALLY_COVERED | Rejected |
| Safe field shadowing unsafe method | STATICALLY_COVERED | Allowed |
| Getter shadowing unsafe function field | STATICALLY_COVERED | Rejected when used unsafely |
| Protected member through static literal access | STATICALLY_COVERED | Rejected |
| Namespace alias chain | STATICALLY_COVERED | Rejected |
| Class expression in object property | NEW_FINDING | Bypassed |
| Constructor factory in object method/property | NEW_FINDING | Bypassed |
| Safe override followed by detached reference | STATICALLY_COVERED | Allowed |
| Mixed static/instance safe chain | STATICALLY_COVERED | Allowed |

Runtime-generated factories, genuinely dynamic constructors, prototype mutation, monkey-patching, symbols, nonliteral imports, `eval`, `new Function`, generated code, and information-erasing casts remain accepted documented static limitations. The NEW_FINDING cases above are not dynamic limitations.

## 11. Safe-control findings

Existing and focused controls remain allowed: `queryLabel`/`queryMetadata`, string `"query"`, acquire-returning functions/getters/methods, safe tuple rest, all-safe spread alternatives, harmless/acquire-only factories, harmless namespace classes, safe static/instance overrides, detached safe references, documentation, and comments. No new false positive was found.

## 12. Regression review

Forbidden-package transitive detection, environment wrappers, direct/aliased P2D.20 checks, caller-role targets, fake allowlist and immutability, coordinates, deterministic suite ordering, cycle termination, deduplication fixtures, and isolated rule coverage pass. Repository live scanning remains clean.

## 13. Scanner implementation findings

- Array/tuple callable registration handles callable elements but does not carry class surfaces into indexed container entries (`scripts/check-core-v2-boundaries.mjs:594-658`).
- Object registration handles functions, nested objects, and arrays but not class-expression values or returned-class surfaces from methods/properties (`scripts/check-core-v2-boundaries.mjs:661-689`).
- Class factories accept inline classes, returned factories, or paths present in `classIdentities`; copied class aliases are not added to that identity set (`scripts/check-core-v2-boundaries.mjs:691-722,794-813`).
- Function factory analysis has no statically resolvable parameter-to-argument class-surface substitution (`scripts/check-core-v2-boundaries.mjs:794-813,905-914`).
- Returned tuple materialization only occurs for a variable initialized directly by a call; a call used directly as a spread source has no returned-container path (`scripts/check-core-v2-boundaries.mjs:594-625,909-914`).
- Source parsing remains non-executing; ordering, path normalization, Windows operation, fixed-point termination, and repository-scale performance are acceptable in the executed review.

## 14. Frozen-contract compatibility

No reviewed artifact changes the P2D.20 eight-field mapping, acquire-only transport, raw result, authority provenance, idempotency, replay, safe errors, outbox, lifecycle, disabled factory, no-service-role fallback, no-dual-write rule, or activation state. No route imports or runtime activation were introduced.

## 15. Documentation accuracy

The counts in A2.4A.12 are accurate: 320 tests, 183 fixtures, and 19/19 stable rule IDs covered. However, its executive statement that all four HIGH findings are corrected within the statically resolvable contract is too broad. Direct fixtures are corrected, but aliased class returns, concrete generic wrappers, constructor containers, and object-held class/factory surfaces remain statically resolvable. Documentation must be narrowed after implementation correction.

## 16. Validation results

- `npx tsc --noEmit`: PASS, zero errors.
- `npm run lint`: PASS, zero errors and six pre-existing warnings.
- Core V2 contract suite: PASS — 320 tests.
- Scanner fixtures: PASS — 183 fixtures.
- Stable rules: 19; isolated coverage: 19/19; uncovered: zero.
- Adapter type tests: PASS.
- Live boundary scanner: PASS.
- `npm run build`: PASS — 114 static pages generated.
- Final `git diff --check` and strict trailing-whitespace results are recorded after creation of this report.

## 17. Findings matrix

| ID | Severity | Path and line | Issue and bypass | Violated contract | Required correction | Commit impact |
|---|---|---|---|---|---|---|
| H-1 | HIGH | `scripts/check-core-v2-boundaries.mjs:691-722,794-813` | `const Alias = Class; function make(){ return Alias }` loses returned class identity and permits unsafe construction. | Every statically resolvable class/constructor alias must preserve generic capability provenance. | Preserve class-identity aliases alongside copied surfaces and resolve factory returns through them; add unsafe/safe fixtures. | Blocks commit. |
| H-2 | HIGH | `scripts/check-core-v2-boundaries.mjs:794-813,905-914` | A concrete `id(Class)`-style wrapper returns a statically known constructor but parameter/argument provenance is lost. | Concretely resolved factory wrappers must fail closed. | Add bounded parameter-to-argument returned-class substitution for statically resolvable local calls; add safe controls. | Blocks commit. |
| H-3 | HIGH | `scripts/check-core-v2-boundaries.mjs:594-658` | A forbidden class stored in a tuple and retrieved by constant index loses its static/instance surface. | Indexed constructor aliases must retain class provenance. | Track class-surface identities per container index and through spread/rest/destructuring/access. | Blocks commit. |
| H-4 | HIGH | `scripts/check-core-v2-boundaries.mjs:661-689` | Class expressions and class-producing methods/properties stored on objects are not registered as class/factory surfaces. | Statically named object properties and methods must preserve class/constructor provenance. | Register class expressions and returned-class factories under object-member paths and propagate aliases/calls. | Blocks commit. |
| M-1 | MEDIUM | `scripts/check-core-v2-boundaries.mjs:520-545,594-625,909-914` | A directly spread returned tuple is blocked only by a source-level finding; the diagnostic omits returned-container, spread, rest, and final exposure steps. | Diagnostics must show the complete tuple/rest/factory provenance chain. | Resolve returned-container paths for call expressions used as spread sources and assert the complete chain. | Required with correction. |
| M-2 | MEDIUM | `runtime-integration/A2.4A.12-TUPLE-REST-ALTERNATIVE-SPREAD-CLASS-RETURN-AND-OVERRIDE-CORRECTION.md:5-7` | The stated correction scope exceeds demonstrated behavior. | Security documentation must match implemented static coverage. | Narrow/update after H-1–H-4 and M-1 are corrected. | Required with correction. |

No BLOCKER was found. Four HIGH and two MEDIUM findings remain unresolved.

## 18. Commit-readiness decision

**Not ready for commit.** The existing suite passes but lacks fixtures for all four confirmed bypasses. Correct H-1–H-4 and M-1, add isolated unsafe and safe controls, update A2.4A.12 accurately, then repeat an independent review.

## 19. Files created

- `runtime-integration/A2.4A.13-FINAL-INDEPENDENT-POST-CORRECTION-REVIEW.md`

## 20. Files modified

- None.

## 21. SQL/database/package proof

- SQL generated/executed: none.
- Database, Supabase, Vercel, or other provider contacted: no.
- Packages installed or manifests changed: none.
- Implementation files modified by this review: none.
- Staging, commit, or push: none.
- Core V2 activation or legacy-path changes: none.

## 22. Exact recommended next phase

Proceed with **A2.4A.14 — Class-Alias, Constructor-Container, Object-Class-Factory, and Returned-Rest Provenance Correction**. Limit it to H-1–H-4, M-1/M-2, focused fixtures, and documentation accuracy; preserve the hard-disabled state and all frozen contracts.

Final verdict: **CORRECTION_REQUIRED**

A24A13_900_FINAL_INDEPENDENT_POST_CORRECTION_REVIEW_COMPLETE
