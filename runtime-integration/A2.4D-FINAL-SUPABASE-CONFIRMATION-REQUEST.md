# A2.4D Final Supabase Confirmation Request

Please answer each item for the applicable hosted AFEX project and tier using exactly one classification: `SUPPORTED`, `UNSUPPORTED`, `PLAN_DEPENDENT`, `UNDOCUMENTED`, or `RUNTIME_TEST_REQUIRED`. Identify the supported operator identity/channel and any relevant limitation. Do not request or include credentials, passwords, connection URLs, project secrets, or business data.

1. Is creation of a short-lived password-bearing custom LOGIN with finite `VALID UNTIL` and finite connection limit supported?
2. Which channel and identity are authorized for `CREATE ROLE`, `ALTER ROLE`, durable LOGIN disablement, session termination, and `DROP ROLE`: Dashboard SQL Editor, direct `psql`, another managed channel, or none?
3. May a disposable custom owner own a harmless SECURITY DEFINER test function and its disposable probe objects?
4. If a disposable owner is unsupported, is assigning the harmless test function to existing `afex_function_owner` supported and recommended, and what restrictions apply?
5. How must active sessions for the disposable LOGIN be enumerated and terminated before cleanup, including Transaction Pooler sessions?
6. What is the supported durable disable/revoke procedure that prevents new LOGIN sessions and exact-function EXECUTE after a partial failure, and how is it verified?
7. Are transaction-scoped advisory locks supported through the project’s Transaction Pooler, and which pinning, failover, or backend-reuse caveats require runtime testing?
8. Are cancellation, statement-timeout, abrupt-disconnect, and reusable-backend cleanup semantics documented for this pooler path? Identify which remain runtime-test-only.
9. What exact Transaction Pooler endpoint class, port, username form, TLS mode, CA source, and hostname/certificate verification are required?
10. What database/pooler connection limits and safe temporary test allocation apply to this project/tier, and which metrics should establish current headroom?
11. Do Dashboard SQL Editor and direct `psql` differ in role-creation, ownership-transfer, policy, function-owner, grant/revoke, or session-termination authority?
12. What supported cleanup order removes sessions, LOGIN capability, EXECUTE, disposable objects, ownership dependencies, and the role without `CASCADE` or Production-object impact?

A positive answer documents provider support only. It does not replace read-only authority proof, isolated runtime evidence, independent review, cleanup attestation, or explicit execution authorization.
