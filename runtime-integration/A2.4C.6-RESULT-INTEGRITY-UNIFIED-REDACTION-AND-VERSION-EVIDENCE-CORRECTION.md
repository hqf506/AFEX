# A2.4C.6 Result-Integrity, Unified Redaction, and Version-Evidence Correction

Decision: `RESULT_INTEGRITY_CORRECTIONS_COMPLETE`

## Corrections

- Reordered runner control flow so every safely retained result pair is digest- and JSON-validated before timeout/native-exit/final-status classification.
- Defined the six exact exit/evidence state-machine outcomes in the evidence contract.
- Replaced recursive/manual result serialization with one authoritative, bounded, operation-specific evidence projection; every derived matrix uses sanitized projected results.
- Added bounded string/array handling and omitted unapproved fields without arbitrary-object traversal.
- Added strict 64-character, single-line version grammar and deterministic lowercase normalization for Node, npm, pg, optional `@types/pg`, and optional `@vercel/functions`; absent optional packages use `NOT_INSTALLED`.
- Added source fixtures for newline, length, invalid-character, optional-sentinel, and unapproved-field rejection.
- Truthfully resolved A2.4C.2 traceability through an index rather than fabricating the missing review.
- Corrected the A2.4C.3 readiness overstatement while preserving its historical content.

## Boundaries

No SQL, runner, harness, package, database, provider, credential, Git write, Production access, P2D.20 call, or Core V2 activation occurred. Independent rereview and all external gates remain required.

## Subsequent narrow correction

A2.4C.7 independently approved the major controls but required complete named redaction fixtures, deterministic runner outcome fixtures, and shared validation for fixed schema/tool versions. A2.4C.8 is the authoritative bounded correction for those remaining findings; this historical record is not rewritten as final approval.
