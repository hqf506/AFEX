# A2.3H.2A Next Decision

## Current decision

- Driver: `PG_PREFERRED_PENDING_RUNTIME_TESTS`
- `pg`: `FUTURE_REQUIRED`, installation blocked.
- `@vercel/functions`: `FUTURE_REQUIRED`, installation blocked.
- Transaction Pooler: provider-recommended candidate, not yet architecture-approved.
- Runtime implementation and activation: blocked.

## Exact next phase

`A2.3H.1 — Submit Remaining Supabase Confirmation Questions`

After a scoped response is received, proceed to:

`A2.3H.2B — Normalize Supabase Provider Response and Re-evaluate G1–G5, G10, and G11`

Do not design executable test SQL or install packages until the response is normalized and conflicts are resolved. Ambiguous answers advance only to reviewed isolated runtime-test design; they do not pass a gate.

## Work still blocked

- SQL execution, role or membership creation, and Production database contact.
- `pg` or `@vercel/functions` installation and package/lockfile changes.
- Credential/environment changes.
- Connection Pool, factory, transaction wrapper, `SET ROLE`, or generic query implementation.
- P2D.20 application calls, Production integration, dual write, service-role fallback, route changes, and feature activation.

## Frozen safety rules

Future implementation must remain domain-specific, hard-disabled, bounded, and free of generic SQL exposure. It must use same-key reconciliation after unknown outcomes and cannot fall back to legacy after a Core V2 send or ambiguous commit.
