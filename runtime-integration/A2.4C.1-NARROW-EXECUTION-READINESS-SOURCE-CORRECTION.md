# A2.4C.1 Narrow Execution-Readiness Source Correction

Decision: HISTORICAL CLAIM; A2.4C.2 FOUND TARGETED CORRECTIONS REMAINING

## Corrections applied

- Expanded the PostgreSQL 17.6 read-only attestation into fail-closed catalog checks for attributes, direct/transitive membership, ownership, exact function identity, overloads, `SECURITY DEFINER`, fixed `search_path`, direct/effective ACLs, default ACL causality, columns, relations, sequences, schemas, unrelated functions, managed-role exposure, and P2D.20 denial.
- Hardened the PowerShell runner with repository and script proof, normalized evidence containment, Git-ignore proof, manifest pinning, exact authorization, bounded process timeout and process-tree termination, stdout/stderr retention, numeric exit recording, marker/evidence validation, safe all-path summary, and environment restoration.
- Added exact semantic assertions for identity before/after transaction boundaries, probe caller/definer, backend PID, denial SQLSTATE, timeout quarantine, lock contention/release, pool exhaustion, sequential contamination, and concurrent isolation.
- Added executable controller contracts with deterministic phases, prerequisites, timeout inputs, expected outcomes, quarantine, evidence, cleanup, and fail-closed placeholders.
- Froze the machine evidence schema and synchronized the runbook, gates, blockers, correction histories, and manifest.

## Preserved boundaries

Setup and cleanup SQL mutation logic, future Production design, the independent A2.4B.4 review, provider questions, operator/provider packages, application code, adapter, routes, packages, environment, and Production behavior were not modified. Core V2 remains hard-disabled.

## External blockers

Provider authority, operator inputs, dependency approval/installation, credentials, all runtime evidence, cleanup proof, and independent approval remain absent. No source result authorizes execution.

## A2.4C.3 correction addendum

A2.4C.3 corrected the incomplete signature/column/owner attestation, early-evidence semantics, digest/JSON verification, bounded termination, evidence redaction and assertion fields, denial matrix, final-status binding, timeout hierarchy, and partial-setup procedure identified by A2.4C.2. Independent rereview remains required.
