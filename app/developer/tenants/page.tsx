import { DeveloperTenantCard } from '@/components/developer-tenant-card'
import { supabaseAdmin } from '@/lib/supabase/admin'
export default async function DeveloperTenantsPage() { const { data } = await supabaseAdmin.from('tenants').select('id,name,created_at').order('created_at', { ascending: false }).limit(100); return <section className="min-w-0 space-y-5"><header className="rounded-[24px] border border-cyan-300/15 bg-white/[0.045] p-5"><h1 className="text-2xl font-black">المنشآت</h1><p className="mt-2 text-sm text-slate-400">أحدث 100 منشأة مسجلة على المنصة. افتح البطاقة لعرض التفاصيل الآمنة.</p></header><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{(data || []).map((tenant) => <DeveloperTenantCard key={tenant.id} tenant={{ id: tenant.id, name: tenant.name, createdAt: tenant.created_at }} />)}</div></section> }
